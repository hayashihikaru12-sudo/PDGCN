# PDGCN 快速训练与验证闭环计划

## 1. 总体目标

当前 PDGCN 训练流程已经完成基础搭建，训练方式以单层 PDGCN 的物理约束损失为核心。现有 `DesignPlan/1.h5` 输入数据包含 `xyz`、`fiber`、`Q`、`edge_index` 和 `boundary_nodes`，但不包含温度真值序列，因此第一版训练闭环采用：

- 物理损失训练：保持现有 PDE residual 与 outflow boundary loss 作为训练目标。
- 仿真真值验证：训练后用外部仿真温度场评估预测准确性。
- 快速分阶段训练：先短跑诊断，再稳定训练，最后长跑确认。

第一版不引入监督损失，不修改 `DesignPlan`、`PIGNN` 或现有训练代码。

## 2. 训练监控方案

训练过程中需要同时监控“优化是否收敛”和“温度场是否合理”，不能只看单一 epoch loss。

建议记录以下指标：

- `loss_total`：总物理损失。
- `loss_pde`：PDE 残差损失。
- `loss_outflow`：出流边界损失。
- `window_losses`：每个 TBPTT 窗口的损失。
- `best_epoch` 与 `best_loss`：用于保存最佳 checkpoint。
- `learning_rate`、epoch 耗时、累计训练耗时。

建议输出以下文件：

- `runs/pdgcn/history.json`：训练历史。
- `runs/pdgcn/checkpoint.pt`：最后一次模型。
- `runs/pdgcn/best_checkpoint.pt`：验证或训练指标最优模型。
- `runs/pdgcn/metrics/metrics.json`：训练和验证指标汇总。
- `runs/pdgcn/figures/loss_curve.png`：loss 曲线。
- `runs/pdgcn/figures/rollout_temperature_step_*.png`：滚动预测温度云图。
- `runs/pdgcn/figures/residual_map_step_*.png`：残差热力图。
- `runs/pdgcn/figures/error_curve.png`：仿真真值对比误差曲线。

可视化建议：

- 每隔固定 epoch 执行一次 rollout。
- 对初始帧、中间帧、末帧输出温度云图。
- 记录最大温度、平均温度、最小温度随时间变化。
- 若出现温度爆炸、NaN、Inf 或明显非物理负温升，应立即停止该组训练并回退参数。

## 3. 仿真真值验证方案

仿真真值只用于验证，不参与第一版训练损失。

仿真数据推荐格式：

- 单层验证：`[time, node, 1]`
- 多层验证：`[time, layer, node, 1]`

其中温度单位应为真实温度单位，与训练配置中的 `T_amb` 和 `delta_T0` 保持一致。若仿真结果已经是无量纲温度，需要在验证元信息中明确标记。

外部网格对齐策略：

- 若仿真节点顺序与训练 HDF5 完全一致，直接逐节点比较。
- 若节点顺序不同但拓扑相同，通过坐标匹配建立节点映射。
- 多层模型中，每一层应使用相同面内拓扑，并通过层号或厚度方向坐标区分。
- 坐标匹配需要设置严格 tolerance；超过 tolerance 的节点应报告为映射失败，不能静默忽略。

验证指标：

- RMSE：整体均方根误差。
- MAE：整体平均绝对误差。
- relative RMSE：相对误差，用于不同温升尺度下比较。
- peak temperature error：峰值温度误差。
- peak location error：峰值位置误差。
- per-step RMSE curve：逐时间步误差曲线。

多层验证时，至少分别输出：

- 顶层温度误差。
- 各层平均误差。
- 全三维结构整体误差。
- 厚度方向温度剖面对比。

## 4. 三阶段训练策略

### 阶段 1：冒烟训练

目标是确认训练链路可跑通，loss 不发散，温度场初步合理。

建议配置：

- `epochs`: 20-50
- `tbptt_window`: 3-5
- `warmup_steps`: 10-30
- `lr`: 1e-4
- `loss_threshold`: 暂保留或设为较宽松值

验收重点：

- 无 NaN/Inf。
- loss 整体有下降趋势。
- rollout 温度范围合理。
- 温度云图中的热源响应位置与 `Q` 分布一致。

### 阶段 2：稳定训练

目标是得到可靠的 best checkpoint，并观察物理损失与仿真误差是否同步改善。

建议配置：

- `epochs`: 200-500
- `tbptt_window`: 5
- `warmup_steps`: 30
- `lr`: 1e-4
- `loss_threshold`: 0.02
- 使用 RTX 3060 GPU。
- 复用已生成的 static cache，避免重复构图开销。

验收重点：

- 保存 `best_checkpoint.pt`。
- 比较 best checkpoint 与 last checkpoint。
- 仿真验证 RMSE 曲线不能随时间持续失控。
- 峰值温度和峰值位置误差应处于可解释范围。

### 阶段 3：最终长跑

目标是在阶段 2 结果稳定后进行完整训练，获得最终模型。

建议配置：

- `epochs`: 1000
- 启用 early stopping。
- 保留 best checkpoint。
- 固定间隔输出验证可视化。
- 训练结束后统一生成完整报告。

不建议一开始直接一步到位长跑。原因是当前训练主要依赖物理损失，物理 loss 下降并不必然等价于真实温度误差下降；长跑前需要先确认温度场形态、rollout 稳定性和仿真真值误差。

## 5. 测试与验收标准

基础训练验收：

- 训练过程无 NaN/Inf。
- loss 曲线整体下降。
- 每个 TBPTT 窗口损失保持有限。
- checkpoint 能正常保存和加载。

温度场验收：

- 预测温度范围合理。
- 温度峰值位置与热源位置和扫描方向一致。
- 滚动预测中温度不会出现无解释爆炸。
- 边界节点满足 Dirichlet 或 outflow 约束预期。

仿真对比验收：

- 能成功完成节点映射或确认节点顺序一致。
- 能输出 RMSE、MAE、relative RMSE、峰值温度误差和峰值位置误差。
- 误差曲线不随时间持续失控。
- best checkpoint 的验证误差应优于或不差于 last checkpoint。

报告验收：

- 生成 loss 曲线。
- 生成预测温度云图。
- 生成真值-预测-误差三联图。
- 生成逐时间步误差曲线。
- 保存 `metrics.json`，便于后续横向比较不同训练配置。

## 6. 默认假设

- `DesignPlan` 和 `PIGNN` 均为参考资料目录，只读不改。
- 第一版训练只做“物理训练 + 仿真验证”，不加入监督损失。
- 仿真真值与训练局部窗口拓扑相同。
- 若仿真节点顺序不同，则通过坐标映射对齐。
- 推理模型是多层相同拓扑网格沿厚度方向堆叠；单层 PDGCN 训练仍聚焦面内热动力学。
- 多层准确性验证以仿真真值为准，优先检查顶层温度、各层平均误差和厚度方向温度剖面。
