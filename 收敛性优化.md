# 收敛性优化

## 目的

硬投影会直接修改 PD-GCN 输出：

$$
\Delta T_{\mathrm{used}}
=
\mathrm{Project}
\left(
\Delta T_{\mathrm{raw}}
\right)
$$

如果训练初期直接启用硬投影，模型输出在净增量正负之间波动时，投影行为会频繁切换，容易造成 loss 抖动和收敛不稳定。

因此，训练阶段默认关闭硬投影，改用软能量约束；推理阶段可再启用保符号投影作为兜底。

## 软能量约束

PD-GCN 的无源面内原始输出为：

$$
\Delta T_i^{\mathrm{raw}},
\quad
i \in \Omega_{\mathrm{int}}
$$

其中 $\Omega_{\mathrm{int}}$ 表示内部节点集合。

当前采样点是均匀的，因此不使用面积权重，内部节点平均增量为普通算术平均：

$$
\overline{\Delta T}^{\mathrm{raw}}
=
\frac{1}{|\Omega_{\mathrm{int}}|}
\sum_{i\in\Omega_{\mathrm{int}}}
\Delta T_i^{\mathrm{raw}}
$$

无源面内项不应产生净升温，因此只惩罚正的平均增量：

$$
L_{\mathrm{energy}}
=
\lambda_E
\left[
\max
\left(
\overline{\Delta T}^{\mathrm{raw}},
0
\right)
\right]^2
$$

当 $\overline{\Delta T}^{\mathrm{raw}}\le 0$ 时，该项为 $0$，不会惩罚正常的热量重新分布或耗散。

## 总损失

加入软能量约束后：

$$
L_{\mathrm{total}}
=
L_{\mathrm{pde}}
+
\lambda_{\mathrm{out}}L_{\mathrm{outflow}}
+
\lambda_{\mathrm{smooth}}L_{\mathrm{smooth}}
+
\lambda_{\mathrm{zero}}L_{\mathrm{zero}}
+
L_{\mathrm{energy}}
$$

其中：

$$
L_{\mathrm{energy}}
=
\lambda_E
\left[
\max
\left(
\frac{1}{|\Omega_{\mathrm{int}}|}
\sum_{i\in\Omega_{\mathrm{int}}}
\Delta T_i^{\mathrm{raw}},
0
\right)
\right]^2
$$

## 训练与推理

训练阶段使用 raw 输出滚动：

$$
\Delta T_{\mathrm{used}}
=
\Delta T_{\mathrm{raw}}
$$

并在损失中加入：

$$
L_{\mathrm{energy}}
$$

推理阶段如果需要兜底，可使用保符号投影：

$$
\Delta T_{\mathrm{used}}
=
\mathrm{Project}_{\mathrm{sign}}
\left(
\Delta T_{\mathrm{raw}}
\right)
$$

## 推荐参数

软能量权重建议从小值开始：

$$
\lambda_E=10^{-4}
$$

若仍存在明显自产热，可逐步增大到：

$$
\lambda_E=10^{-3}\sim 10^{-2}
$$

过大的 $\lambda_E$ 可能压制面内输运学习，使模型更倾向于输出接近 $0$ 的增量。

## 当前代码配置

训练阶段默认配置为：

```json
"energy_conservation_weight": 0.0001,
"non_heating_projection": false
```

含义是：

1. 不在训练滚动中使用硬投影。
2. 使用软能量约束抑制无源自产热。
3. `loss_energy` 会写入训练历史和监控指标。
