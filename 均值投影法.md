# 均值投影法

## 目的

均值投影法用于抑制无源 PD-GCN 的“自产热”现象。

在当前架构中，总更新为：

$$
T^{n+1}=T^n+\Delta T_{\mathrm{source}}+\Delta T_{\mathrm{inplane}}+\Delta T_{\mathrm{thickness}}
$$

其中 PD-GCN 只负责无源面内输运项：

$$
\Delta T_{\mathrm{inplane}}
$$

因此，在无热源条件下，PD-GCN 不应让内部节点的平均温度增量为正。

## 基本思想

设模型原始输出为：

$$
\Delta T_i^{\mathrm{raw}}, \quad i \in \Omega_{\mathrm{int}}
$$

其中 $\Omega_{\mathrm{int}}$ 表示内部节点集合，不包含入口、侧边和出口边界节点。

内部节点平均增量为：

$$
\overline{\Delta T}^{\mathrm{raw}}
=
\frac{1}{|\Omega_{\mathrm{int}}|}
\sum_{i \in \Omega_{\mathrm{int}}}
\Delta T_i^{\mathrm{raw}}
$$

如果该平均值为正，说明 PD-GCN 面内项正在产生净升温。均值投影法扣除这个正偏置：

$$
c=\max\left(\overline{\Delta T}^{\mathrm{raw}},0\right)
$$

$$
\Delta T_i^{\mathrm{proj}}
=
\Delta T_i^{\mathrm{raw}}-c,
\quad i \in \Omega_{\mathrm{int}}
$$

于是投影后的内部平均增量满足：

$$
\overline{\Delta T}^{\mathrm{proj}} \le 0
$$

也就是说，PD-GCN 可以重新分配热量，但不能让无源面内项凭空增加平均温度。

## 使用位置

均值投影只作用于 PD-GCN 输出的面内增量：

$$
\Delta T_{\mathrm{inplane}}
$$

不应作用于显式热源项：

$$
\Delta T_{\mathrm{source}}
$$

推荐流程为：

$$
T_{\mathrm{source}} = T^n + \Delta T_{\mathrm{source}}
$$

$$
\Delta T_{\mathrm{inplane}}^{\mathrm{raw}}
=
\mathrm{PDGCN}(T_{\mathrm{source}})
$$

$$
\Delta T_{\mathrm{inplane}}^{\mathrm{proj}}
=
\mathrm{Project}
\left(
\Delta T_{\mathrm{inplane}}^{\mathrm{raw}}
\right)
$$

$$
T_{\mathrm{inplane}}
=
T_{\mathrm{source}}
+
\Delta T_{\mathrm{inplane}}^{\mathrm{proj}}
$$

最后再进入厚度方向 FDM：

$$
T^{n+1}
=
\mathrm{FDM}_{z}
\left(
T_{\mathrm{inplane}}
\right)
$$

## 简化代码

```python
def project_non_heating_delta(delta, boundary_nodes):
    internal_mask = build_internal_mask(delta.shape[0], boundary_nodes)
    projected = delta.clone()
    mean_delta = projected[internal_mask].mean()
    correction = torch.relu(mean_delta)
    projected[internal_mask] = projected[internal_mask] - correction
    return projected
```

训练和推理中都建议使用同一投影逻辑，避免训练行为和滚动预测行为不一致。

当前代码通过模型配置项开启该方法：

```json
"non_heating_projection": true
```

## 注意事项

1. 该方法禁止的是“无源净升温”，不是禁止局部节点升温。
2. 局部冷点因面内扩散升温是合理的，只要整体平均不增加。
3. 如果节点采样不均匀，应使用面积加权平均：

$$
\overline{\Delta T}^{\mathrm{raw}}
=
\frac{
\sum_{i \in \Omega_{\mathrm{int}}} A_i \Delta T_i^{\mathrm{raw}}
}{
\sum_{i \in \Omega_{\mathrm{int}}} A_i
}
$$

4. 均值投影是硬约束，效果通常比单纯增加零源损失权重更稳定。
5. 它不能替代热源单位、边界条件和 FDM 系数检查；若 $\Delta T_{\mathrm{source}}$ 或 $\Delta T_{\mathrm{thickness}}$ 本身异常，仍需单独排查。
