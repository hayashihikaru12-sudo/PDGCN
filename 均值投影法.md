# 均值投影法

> 说明：本文档保留为历史方案说明。当前代码已将硬投影实现替换为“保符号投影”，训练阶段默认关闭硬投影，并通过 `energy_conservation_weight` 使用软能量约束。

## 目的

均值投影法用于抑制无源 PD-GCN 的“自产热”现象。

总体更新式为：

$$
T^{n+1}
=
T^n
+
\Delta T_{\mathrm{source}}
+
\Delta T_{\mathrm{inplane}}
+
\Delta T_{\mathrm{thickness}}
$$

其中 PD-GCN 只负责无源面内输运：

$$
\Delta T_{\mathrm{inplane}}
$$

因此，PD-GCN 不应让内部节点平均温度增量为正。

## 方法

设 PD-GCN 原始输出为：

$$
\Delta T_i^{\mathrm{raw}},
\quad
i\in\Omega_{\mathrm{int}}
$$

当前采样点是均匀的，因此内部平均增量为普通算术平均：

$$
\overline{\Delta T}^{\mathrm{raw}}
=
\frac{1}{|\Omega_{\mathrm{int}}|}
\sum_{i\in\Omega_{\mathrm{int}}}
\Delta T_i^{\mathrm{raw}}
$$

若平均增量为正，则扣除该正偏置：

$$
c=
\max
\left(
\overline{\Delta T}^{\mathrm{raw}},
0
\right)
$$

$$
\Delta T_i^{\mathrm{proj}}
=
\Delta T_i^{\mathrm{raw}}-c
$$

投影后满足：

$$
\overline{\Delta T}^{\mathrm{proj}}\le 0
$$

## 使用位置

投影只作用于 PD-GCN 的面内增量，不作用于显式热源：

$$
T_{\mathrm{source}}
=
T^n+\Delta T_{\mathrm{source}}
$$

$$
\Delta T_{\mathrm{inplane}}^{\mathrm{raw}}
=
\mathrm{PDGCN}
\left(
T_{\mathrm{source}}
\right)
$$

$$
\Delta T_{\mathrm{inplane}}^{\mathrm{proj}}
=
\mathrm{Project}
\left(
\Delta T_{\mathrm{inplane}}^{\mathrm{raw}}
\right)
$$

$$
T_{\mathrm{inplane}}
=
T_{\mathrm{source}}
+
\Delta T_{\mathrm{inplane}}^{\mathrm{proj}}
$$

## 局限

均值投影会对所有内部节点扣除同一个偏置，可能把原本应升温的低温节点扣成负增量：

$$
\Delta T_i^{\mathrm{raw}}>0
\quad
\text{but}
\quad
\Delta T_i^{\mathrm{proj}}<0
$$

因此当前代码不再使用该方法，而是使用保符号投影作为硬投影实现。

当前默认训练配置为：

```json
"energy_conservation_weight": 0.0001,
"non_heating_projection": false
```

若显式设置：

```json
"non_heating_projection": true
```

实际启用的是保符号投影，不是本文档中的均值扣除投影。
