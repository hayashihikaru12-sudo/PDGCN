# 加零源锚定损失法说明

## 目的

零源锚定损失用于消除单层 PDGCN 在无热源、冷态或均匀温度场下的自生热现象。

对于无源面内输运模块，若当前温度为环境温度，且外部热源为零，则物理上不应产生任何温度增量：

$$
Q_i = 0,\quad T_i^* = 0
\quad \Longrightarrow \quad
\Delta T_{\mathrm{PDGCN},i}^* = 0
$$

其中：

- $T_i^*=(T_i-T_{amb})/\Delta T_0$；
- $Q_i$ 为节点表面热流；
- $\Delta T_{\mathrm{PDGCN},i}^*$ 为 PDGCN 输出的无源面内温度增量。

## 损失定义

构造一个零源冷态图 $\mathcal{G}_0$：

$$
Q_i^* = 0,\qquad T_i^* = 0
$$

将其输入 PDGCN，得到：

$$
\Delta T_{0,i}^* = f_\theta(\mathcal{G}_0)_i
$$

零源锚定损失定义为：

$$
\mathcal{L}_{cold}
=
\frac{1}{|\Omega_{int}|}
\sum_{i\in\Omega_{int}}
\left(\Delta T_{0,i}^*\right)^2
$$

其中 $\Omega_{int}$ 为内部节点集合，避免边界强制温度节点干扰统计。

## 总损失接入方式

原始训练损失可写为：

$$
\mathcal{L}_{base}
=
\mathcal{L}_{PDE}
+ \lambda_{out}\mathcal{L}_{outflow}
+ \lambda_{smooth}\mathcal{L}_{smooth}
$$

加入零源锚定后：

$$
\mathcal{L}_{total}
=
\mathcal{L}_{base}
+ \lambda_{cold}\mathcal{L}_{cold}
$$

推荐从较小权重开始：

$$
\lambda_{cold}=0.1\sim 1.0
$$

若 Q=0 测试仍明显升温，可逐步增大。

## 实现要点

1. 在每个训练窗口中额外构造一次冷态输入：

   $$
   T^*=0,\qquad Q^*=0
   $$

2. 使用相同的几何、纤维方向、边特征和速度条件。
3. 只惩罚 PDGCN 的输出增量，不经过显式热源模块。
4. 损失建议只统计内部节点：

   $$
   i\in\Omega_{int}
   $$

5. 该损失应同时覆盖 Q=0 训练样本和人工冷态样本。

## 预期效果

加入后，模型会学习到零源冷态不产生温升：

$$
f_\theta(\mathcal{G}_0)\approx 0
$$

验收指标建议为：

$$
\mathrm{mean}_{i\in\Omega_{int}}
\left|\Delta T_{0,i}^*\right|
\ll 1
$$

在当前温度尺度下，若 $\Delta T_0=230^\circ C$，则：

$$
\Delta T^*=0.01
\quad \Longleftrightarrow \quad
\Delta T\approx 2.3^\circ C
$$

因此 Q=0 单步平均温升最好压到 $1^\circ C$ 以下，即：

$$
\mathrm{mean}(|\Delta T_{0}^*|) < \frac{1}{230}
$$

