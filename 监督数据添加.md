# 监督数据添加

## 1. 当前目标

在单层固定拓扑 PDGCN 训练中接入 FEM 温度监督，使模型在相同网格、热源、材料参数和边界条件下逼近 FEM 单步温度演化。

物理职责保持不变：

$$
T_{n+1}^{*}
=
T_n^{*}
+
\Delta T_{\mathrm{source},n}^{*}
+
\Delta T_{\mathrm{inplane},n}^{*}.
$$

- $\Delta T_{\mathrm{source}}^{*}$ 由显式表面热源模块根据 `dynamic/Q` 计算；
- $\Delta T_{\mathrm{inplane}}^{*}$ 由无源 PDGCN 预测；
- FEM 温度只作为监督标签；
- PDGCN 节点输入仍为

$$
\left[x^{*},y^{*},z^{*},f_x,f_y,f_z,T^{*}\right].
$$

## 2. HDF5 数据格式

监督数据位于 HDF5 的 `fem/` 组：

```text
fem/temperature          [T, N, 1]
fem/valid_mask           [T, N, 1]  可选
fem/temperature_unit     scalar string，可选，degC/C/K
fem/time                 [T]，可选
fem/metadata_json        scalar string，可选
```

要求：

- `fem/temperature` 与 `dynamic/Q` 形状一致；
- 节点编号和帧索引与 `dynamic/xyz` 对齐；
- `fem/valid_mask` 缺失时使用全 1 mask；
- 代码只校验 `fem/temperature_unit`，不自动转换摄氏/开尔文。

FEM 温度以真实物理温度保存，训练时无量纲化：

$$
T_{\mathrm{FEM}}^{*}
=
\frac{T_{\mathrm{FEM}}-T_{\mathrm{amb}}}{\Delta T_0}.
$$

## 3. 配置

训练配置顶层字段：

```json
"supervision": {
  "enabled": false,
  "temperature_dataset": "fem/temperature",
  "valid_mask_dataset": "fem/valid_mask",
  "lambda_temperature": 1.0,
  "mode": "teacher_forcing"
}
```

当前 v1 只支持 `teacher_forcing`。监督关闭时，原无监督物理残差训练行为保持不变。

## 4. 训练流程

监督训练使用 transition：

```text
n = 0, 1, ..., T - 2
```

teacher forcing 输入为：

$$
T_{\mathrm{input},n}^{*}
=
T_{\mathrm{FEM},n}^{*}.
$$

显式热源后：

$$
T_{\mathrm{source},n}^{*}
=
\mathcal B
\left(
T_{\mathrm{input},n}^{*}
+
\Delta T_{\mathrm{source},n}^{*}
\right).
$$

PDGCN 预测：

$$
T_{\mathrm{pred},n+1}^{*}
=
\mathcal B
\left(
T_{\mathrm{source},n}^{*}
+
\mathrm{PDGCN}
\left(
G_n(T_{\mathrm{source},n}^{*})
\right)
\right).
$$

其中 $\mathcal B$ 为 Dirichlet 边界钳制。监督启用时，`warmup_steps` 不参与训练输入生成。

## 5. 损失

温度监督项：

$$
\mathcal{L}_{T}
=
\frac{
\sum_{i=1}^{N}
m_{n+1,i}
\left(
T_{\mathrm{pred},n+1,i}^{*}
-
T_{\mathrm{FEM},n+1,i}^{*}
\right)^2
}{
\sum_{i=1}^{N}m_{n+1,i}+\varepsilon
}.
$$

总损失：

$$
\mathcal L_{\mathrm{total}}
=
\mathcal L_{\mathrm{physics}}
+
\lambda_T\mathcal L_T.
$$

其中：

$$
\mathcal L_{\mathrm{physics}}
=
\mathcal L_{\mathrm{pde}}
+
\lambda_{\mathrm{out}}\mathcal L_{\mathrm{outflow}}
+
\lambda_{\mathrm{smooth}}\mathcal L_{\mathrm{smooth}}.
$$

当前实现只保留温度场监督，不额外加入 $\mathcal L_{\Delta T}$。

## 6. 监控字段

history 和 monitor 记录：

```text
loss_total
loss_physics
loss_supervised
loss_temperature
fem_temperature_rmse
fem_temperature_mae
fem_temperature_max_error
```

监督快照可包含：

```text
fem_temperature
pred_temperature
temperature_error
```

## 7. 已实现位置

```text
data/static_cache.py          FEM 温度和 mask 读取
training/run_config.py        SupervisionRunConfig
training/train_entry.py       将 supervision 传入 reader 和训练循环
training/static_topology.py   teacher forcing 监督训练路径
training/monitor.py           监督指标和快照字段
configs/README.md             配置说明
configs/pdgcn_train.example.json
```

## 8. 验收标准

- 启用监督训练后，`loss_temperature` 为有限数并能随训练下降；
- 单步 $T_{\mathrm{pred},n+1}$ 与 $T_{\mathrm{FEM},n+1}$ 的误差小于无监督模型；
- PDGCN 输入维度保持 7；
- `dynamic/Q` 和 FEM 温度均不作为额外节点特征进入 PDGCN。
