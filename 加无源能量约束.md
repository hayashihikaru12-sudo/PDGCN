# 加无源能量约束

## 目的

无源能量约束用于限制 PDGCN 在无源面内输运过程中凭空增加系统总热量。

当前职责划分中，PDGCN 只负责：

$$
\Delta T_{\mathrm{inplane}}
$$

热源温升由显式模块计算：

$$
\Delta T_{\mathrm{source}}
$$

因此，在无源 PDGCN 步中，网络可以重新分布温度，但不应制造净热量。

## 物理含义

对于无源面内输运，若忽略边界流入流出，内部区域的总热量不应增加：

$$
\sum_{i\in\Omega_{int}} c_i \Delta T_{\mathrm{PDGCN},i}^*
\le 0
$$

其中：

- $\Omega_{int}$ 为内部节点集合；
- $c_i$ 为节点热容量权重。若没有节点面积或体积权重，可先取 $c_i=1$；
- $\Delta T_{\mathrm{PDGCN},i}^*$ 为网络输出的无源面内温度增量。

若暂时不使用权重，则简化为：

$$
\frac{1}{|\Omega_{int}|}
\sum_{i\in\Omega_{int}}
\Delta T_{\mathrm{PDGCN},i}^*
\le 0
$$

## 损失定义

先计算内部节点平均无源增量：

$$
\overline{\Delta T}_{int}^*
=
\frac{1}{|\Omega_{int}|}
\sum_{i\in\Omega_{int}}
\Delta T_{\mathrm{PDGCN},i}^*
$$

只惩罚正的净温升：

$$
\mathcal{L}_{energy}
=
\left[
\max\left(0,\overline{\Delta T}_{int}^*\right)
\right]^2
$$

使用 ReLU 写法为：

$$
\mathcal{L}_{energy}
=
\mathrm{ReLU}
\left(
\overline{\Delta T}_{int}^*
\right)^2
$$

如果有节点面积或体积权重 $w_i$，可改为加权形式：

$$
\overline{\Delta T}_{w}^*
=
\frac{\sum_{i\in\Omega_{int}} w_i\Delta T_{\mathrm{PDGCN},i}^*}
{\sum_{i\in\Omega_{int}} w_i}
$$

$$
\mathcal{L}_{energy}
=
\mathrm{ReLU}
\left(
\overline{\Delta T}_{w}^*
\right)^2
$$

## 总损失接入方式

原始损失为：

$$
\mathcal{L}_{base}
=
\mathcal{L}_{PDE}
+ \lambda_{out}\mathcal{L}_{outflow}
+ \lambda_{smooth}\mathcal{L}_{smooth}
$$

加入无源能量约束后：

$$
\mathcal{L}_{total}
=
\mathcal{L}_{base}
+ \lambda_{energy}\mathcal{L}_{energy}
$$

推荐初始权重：

$$
\lambda_{energy}=0.1\sim 1.0
$$

如果 Q=0 回归中仍存在平均温度持续上升，可逐步增大。

## 与零源锚定损失的区别

零源锚定损失要求冷态零源时每个节点增量接近零：

$$
\Delta T_i^* \approx 0
$$

无源能量约束只限制整体净增热：

$$
\sum_i \Delta T_i^* \le 0
$$

因此，无源能量约束允许局部节点升温，只要其他区域相应降温，满足热量重新分布。

## 实现要点

1. 该约束应作用于 PDGCN 输出的 $\Delta T_{\mathrm{PDGCN}}^*$，而不是作用于显式热源后的总温升。
2. 统计节点建议使用内部节点 $\Omega_{int}$。
3. 若没有节点面积或体积权重，先使用均匀权重 $w_i=1$。
4. 对 Q=0 文件应重点监控：

   $$
   \overline{\Delta T}_{int}^*
   \le 0
   $$

5. 该方法不能保证每个节点不自生热，所以最好与零源锚定损失配合使用。

## 预期效果

加入该约束后，Q=0 或无源区域的平均温度不应持续抬升：

$$
\frac{1}{|\Omega_{int}|}
\sum_{i\in\Omega_{int}}
T_i^{*(t+1)}
\le
\frac{1}{|\Omega_{int}|}
\sum_{i\in\Omega_{int}}
T_i^{*(t)}
$$

它主要抑制“整体漂移式升温”，对局部尖峰仍需要配合图梯度平滑、零源锚定或通量守恒结构处理。

