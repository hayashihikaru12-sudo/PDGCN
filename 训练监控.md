# 训练监控方案

## 1. 监控目标

训练监控用于在 PDGCN 物理约束训练过程中自动记录优化状态、温度场状态和空间残差分布，避免只依赖单一 epoch loss 判断训练质量。

该方案覆盖：

- 损失值监控：总损失、PDE 残差损失、出流边界损失、β 热耗散项。
- 节点温度统计：均值、最大值、最小值、方差。
- 过程可视化：损失曲线、PDE 残差空间热图、时间帧温度场热图。
- 切片级监控：每完成一个 HDF5 切片训练后，固定评估首个 HDF5 切片并输出指标与图表。

## 2. 输出指标

### 2.1 损失指标

训练过程中记录以下损失分量：

| 指标 | 含义 | 是否参与反向传播 |
| --- | --- | --- |
| `loss_total` | 总物理损失 | 是 |
| `loss_pde` | PDE 残差损失 | 是 |
| `loss_outflow` | 出流边界损失 | 是，经 `lambda_outflow` 加权后进入总损失 |
| `loss_beta` | β 热耗散/正则化项的监控损失 | 否 |

总损失保持现有训练目标：

```text
loss_total = loss_pde + lambda_outflow * loss_outflow
```

β 项只作为监控分量记录，不额外加入 `loss_total`，避免改变当前模型优化目标。

### 2.2 温度统计指标

每个训练窗口结束后，基于当前预测温度场统计节点温度：

| 指标 | 含义 |
| --- | --- |
| `temperature_mean` | 节点温度均值 |
| `temperature_max` | 节点温度最大值 |
| `temperature_min` | 节点温度最小值 |
| `temperature_var` | 节点温度方差 |

温度统计建议输出为真实物理温度单位；PDE 残差仍保持无量纲形式。

## 3. 可视化输出

### 3.1 损失曲线

输出文件：

```text
runs/pdgcn/figures/loss_curve.png
```

图中包含：

- `loss_total`
- `loss_pde`
- `loss_outflow`
- `loss_beta`

横轴为 epoch，纵轴为损失值，并包含标题、坐标轴标签和图例。

### 3.2 PDE 残差空间分布热图

输出文件命名建议：

```text
runs/pdgcn/figures/residual_epoch_XXXX_frame_YYYY.png
```

绘图方式：

- 使用节点三维坐标作为空间位置。
- 使用节点 PDE residual 作为颜色值。
- 添加 colorbar、坐标轴标签和标题。

### 3.3 时间帧温度场热图

输出文件命名建议：

```text
runs/pdgcn/figures/temperature_epoch_XXXX_frame_YYYY.png
```

绘图方式：

- 使用节点三维坐标作为空间位置。
- 使用节点温度作为颜色值。
- 添加 colorbar、坐标轴标签和标题。

默认保存中间帧；也可通过配置指定具体帧索引。

## 4. 切片级监控

训练数据目录中的每个 HDF5 文件视为一个独立切片序列。切片级监控采用以下规则：

1. 每完成一个当前 HDF5 切片训练后触发一次监控。
2. 监控对象固定为目录自然排序后的第一个 HDF5 文件，即首切片。
3. 使用当前模型参数对首切片执行无梯度 rollout。
4. 计算首切片完整平均损失分量。
5. 生成首切片对应的损失曲线、残差热图和温度热图。

建议输出：

```text
runs/pdgcn/figures/first_slice_loss_curve.png
runs/pdgcn/figures/first_slice/residual_epoch_XXXX_after_slice_YYY.png
runs/pdgcn/figures/first_slice/temperature_epoch_XXXX_after_slice_YYY.png
```

该设计用于观察模型在训练不同切片后，对固定首切片物理一致性的变化，便于横向对比。

## 5. 配置方案

建议在训练 JSON 顶层增加可选 `monitoring` 配置：

```json
{
  "monitoring": {
    "enabled": true,
    "interval_epochs": 10,
    "temperature_frame_index": null,
    "figures_dir": null,
    "metrics_path": null
  }
}
```

字段说明：

| 字段 | 默认值 | 含义 |
| --- | --- | --- |
| `enabled` | `true` | 是否启用训练监控 |
| `interval_epochs` | `10` | 每隔多少个 epoch 保存一次过程热图 |
| `temperature_frame_index` | `null` | 温度热图帧索引；`null` 表示中间帧 |
| `figures_dir` | `null` | 图表输出目录；`null` 表示使用 `history.json` 同级的 `figures/` |
| `metrics_path` | `null` | 监控指标输出路径；`null` 表示使用 `history.json` 同级的 `metrics/monitor_history.json` |

## 6. 输出文件组织

默认输出目录结构建议如下：

```text
runs/pdgcn/
├── checkpoint.pt
├── history.json
├── metrics/
│   └── monitor_history.json
└── figures/
    ├── loss_curve.png
    ├── residual_epoch_XXXX_frame_YYYY.png
    ├── temperature_epoch_XXXX_frame_YYYY.png
    ├── first_slice_loss_curve.png
    └── first_slice/
        ├── residual_epoch_XXXX_after_slice_YYY.png
        └── temperature_epoch_XXXX_after_slice_YYY.png
```

其中：

- `history.json` 保存训练主历史。
- `monitor_history.json` 保存更完整的监控指标。
- `figures/` 保存训练过程图表。

## 7. 验证重点

实现后需验证：

- `loss_total` 不包含额外 β 项，训练目标不被改变。
- `loss_beta` 在 `thermal_loss_beta=0` 时为 0。
- epoch history 中包含全部损失分量和温度统计。
- 每隔配置的 epoch 自动生成残差图和温度图。
- 每完成一个 HDF5 切片训练后，会自动评估首切片并生成对应图表。
- 所有 PNG 文件存在且非空。

推荐验证命令：

```powershell
D:\ProgramData\CondaEnv\PIGNN\python.exe -m unittest training.tests.test_static_topology -q
D:\ProgramData\CondaEnv\PIGNN\python.exe -m unittest training.tests.test_run_config -q
```

若环境中安装了 `pytest`，可进一步运行：

```powershell
D:\ProgramData\CondaEnv\PIGNN\python.exe -m pytest
```
