# PDGCN 带符号边方向对流项导致系统性高估的根因分析

## 1. 问题现象

| 指标 | 数值 | 含义 |
|------|------|------|
| Bias | +667.2 °C | 预测温度系统性高于 FEM 真实值 |
| R² | -33.875 | 模型预测能力极差，不如直接取均值 |
| MAPE | 104.9% | 预测误差超过测量值本身 |

**视觉特征**：所有 NN 预测数据点均位于 y=x 对角线上方，构成纯粹的系统性高估（而非随机误差）。

![对比图](D:\腾讯电脑管家截图文件\局部截取_20260702_122940.png)

---

## 2. 相关代码位置

| 文件 | 行号 | 功能 |
|------|------|------|
| `PDGCN/pde/residual.py` | 99–129 | `_signed_directional_convection`：核心对流项离散化 |
| `PDGCN/pde/residual.py` | 6–96 | `compute_pde_residual`：完整 PDE 残差装配 |
| `PDGCN/pde/loss.py` | 112–249 | `total_loss`：物理损失函数聚合 |
| `PDGCN/data/feature_builder.py` | 93–122 | `build_edge_features`：边特征构建（cos_theta 计算） |
| `PDGCN/data/velocity.py` | 30–47 | `tangent_velocity_direction`：速度切平面投影 |

---

## 3. 当前对流项实现

### 3.1 代码

```python
# residual.py _signed_directional_convection (lines 99–129)
def _signed_directional_convection(T_eval_2d, v_scan, sender, receiver,
                                    distance, cos_theta, *, eps: float):
    inv_distance = distance.reciprocal()
    weight_sum = torch.zeros(...)
    weight_sum.index_add_(0, receiver, inv_distance)
    alpha = inv_distance / weight_sum[receiver].clamp_min(eps)  # 距离倒数归一化权重

    edge_contribution = (
        alpha.reshape(1, -1)
        * v_scan
        * cos_theta.reshape(1, -1)
        * (T_receiver - T_sender)
        / distance.reshape(1, -1)
    )

    convection = torch.zeros_like(T_eval_2d)
    convection.index_add_(1, receiver, edge_contribution)
    return convection
```

### 3.2 数学形式

每条边 $i \leftarrow j$ 对接收节点 $i$ 的对流贡献为：

$$
\alpha_{ij} \cdot v_{scan} \cdot \cos\theta_{ij} \cdot \frac{T_i - T_j}{d_{ij}}
$$

其中：

- $\cos\theta_{ij} = \mathbf{\hat{v}}_i \cdot \mathbf{e}_{ij}$：接收节点切向速度方向与边方向（sender → receiver）的点积
- $\mathbf{e}_{ij} = \frac{\mathbf{x}_i - \mathbf{x}_j}{d_{ij}}$：边方向单位向量
- $\alpha_{ij} = \frac{1/d_{ij}}{\sum_{k \in \mathcal{N}(i)} 1/d_{ik}}$：距离倒数归一化权重

节点 $i$ 的对流项：

$$
R_{conv,i} = \sum_{j \in \mathcal{N}(i)} \alpha_{ij} (\mathbf{v}_i \cdot \mathbf{e}_{ij}) \frac{T_i - T_j}{d_{ij}}
$$

### 3.3 修改历程

- **原始方法**：使用 $\mathrm{ReLU}(\cos\theta_{ij})$ 截断反方向邻居
- **当前方法（修改后）**：删除 ReLU，使用带符号 $\cos\theta_{ij}$，保留上下游贡献

---

## 4. 根因分析

### 4.1 根因一（核心数学缺陷）：投影乘积之和 ≠ 向量点积

#### 4.1.1 数学推导

图边差分对 $\frac{T_i - T_j}{d_{ij}}$ 近似的是**沿边方向的温度方向导数**：

$$
\frac{T_i - T_j}{d_{ij}} \approx \nabla T_i \cdot \mathbf{e}_{ij}
$$

因此每条边的实际贡献为：

$$
(\mathbf{v}_i \cdot \mathbf{e}_{ij}) \cdot (\nabla T_i \cdot \mathbf{e}_{ij})
$$

这是**投影的乘积**，而非所需的**向量点积** $\mathbf{v}_i \cdot \nabla T_i$。

#### 4.1.2 展开到张量形式

对节点 $i$ 的所有邻居求和：

$$
\begin{aligned}
\sum_j \alpha_j (\mathbf{v} \cdot \mathbf{e}_j)(\nabla T \cdot \mathbf{e}_j)
&= \sum_j \alpha_j \left( \sum_k v_k e_j^{(k)} \right) \left( \sum_l \frac{\partial T}{\partial x_l} e_j^{(l)} \right) \\[6pt]
&= \sum_k \sum_l v_k \frac{\partial T}{\partial x_l}
\underbrace{\left[ \sum_j \alpha_j e_j^{(k)} e_j^{(l)} \right]}_{M_{kl}}
\end{aligned}
$$

**紧框架条件（Resolution of Identity）**：只有当

$$
\mathbf{M} = \sum_{j \in \mathcal{N}(i)} \alpha_{ij} \mathbf{e}_{ij} \mathbf{e}_{ij}^T = \mathbf{I}_{3\times3}
$$

（单位矩阵）时，近似才严格等于 $\mathbf{v} \cdot \nabla T$。该条件等价于：

$$
\begin{cases}
\sum_j \alpha_j (e_j^{(k)})^2 = 1 & \forall k \in \{x, y, z\} \\[4pt]
\sum_j \alpha_j e_j^{(k)} e_j^{(l)} = 0 & \forall k \neq l
\end{cases}
$$

#### 4.1.3 定量分析：~2× 低估

对于典型的高质量三角形网格（内部节点约 6 邻居，近似等边三角形），边方向均匀分布在 $0°, 60°, 120°, 180°, 240°, 300°$：

**使用等权重 $\alpha_j = 1/6$：**

| 分量 | 计算过程 | 结果 |
|------|----------|------|
| $\sum \alpha \cos^2\theta$ | $\frac{1}{6}(1 + 0.25 + 0.25 + 1 + 0.25 + 0.25)$ | **0.5** |
| $\sum \alpha \sin^2\theta$ | $\frac{1}{6}(0 + 0.75 + 0.75 + 0 + 0.75 + 0.75)$ | **0.5** |
| $\sum \alpha \cos\theta\sin\theta$ | $\frac{1}{6}(0 + 0.433 - 0.433 + 0 + 0.433 - 0.433)$ | **0** |

因此 $\mathbf{M} \approx 0.5 \cdot \mathbf{I}_{2\times2}$（对于切平面内的 2D 子空间）。

**结论：在当前图结构上，对流项被系统性低估了约 2 倍。**

> **注意**：实际网格非理想等边三角形，且使用了距离倒数归一化权重而非等权重，因此实际缩放因子可能偏离 2.0，但量级相近。

---

### 4.2 根因二（物理后果）：对流冷却减半 → 峰值温度翻倍

#### 4.2.1 能量平衡分析

在准稳态移动热源问题中，能量注入与对流散热之间存在平衡：

$$
\underbrace{\mathbf{v} \cdot \nabla T}_{\text{对流冷却}} \;\approx\; \underbrace{\alpha \nabla^2 T}_{\text{扩散传热}} \;+\; \underbrace{Q_{source}}_{\text{热源}}
$$

#### 4.2.2 PDE 残差中的偏差传播

PDGCN 的 PDE 残差为：

$$
R = \frac{\partial T}{\partial t} + \underbrace{R_{conv}}_{\approx\,0.5\,\mathbf{v}\cdot\nabla T} - \frac{1}{Pe}\nabla^2 T
$$

模型最小化 $R^2$，学习到的是使**减半的对流项**满足 $R \approx 0$ 的温度场解。

物理后果：
1. **对流冷却效应减半**：扫描速度带来的"冷材料流入"效应只有真实物理的 50%
2. **热能积累翻倍**：相同热输入下，热量无法被对流有效带走
3. **峰值温升倍增**：$(T_{peak} - T_{amb})_{NN} \approx 2 \times (T_{peak} - T_{amb})_{FEM}$

#### 4.2.3 与观测数据的一致性

图中观测：
- FEM 峰值约 **700–800 °C** 时，NN 预测约 **1300–1500 °C**
- Bias = **+667.2 °C** ≈ FEM 峰值温升的量级
- 即 NN 预测的温升约为 FEM 的 **2 倍**

这与 ~2× 对流低估的分析完全吻合。

---

### 4.3 根因三（加剧因素）：图扩散项精度不足

扩散项使用简化的边基拉普拉斯算子：

```python
# residual.py:89-92
diffusion_edge = k_edge * (T_j - T_i) / distance.square()
diffusion.index_add_(1, receiver, diffusion_edge)
```

即：$\sum_j k_{ij} \frac{T_j - T_i}{d_{ij}^2}$

在非结构化网格上，$\sum_j \frac{T_j - T_i}{d_{ij}^2}$ **不是** $\nabla^2 T$ 的一致逼近。与对流项类似，也受边方向分布不均匀的影响。扩散项的不准确进一步加剧了 PDE 残差与物理真实之间的偏差。

---

### 4.4 根因四（次因）：距离倒数权重无法修复本质缺陷

当前使用的 $\alpha_{ij} = \frac{1/d_{ij}}{\sum_k 1/d_{ik}}$ 只能改变邻居之间的权重分配比例，但：
- 不能将"投影乘积"变为"向量点积"
- 不能填补缺失的 $\times 2$ 因子
- 对非均匀网格的适应性有限

---

## 5. 改进方案

### 5.1 方案 A（推荐快速修复）：卷积缩放补偿因子

#### 实现

在 `_signed_directional_convection` 中乘以可配置补偿因子：

```python
# PDGCN/pde/residual.py _signed_directional_convection 函数末尾
convection = convection * convection_scale  # 补偿紧框架条件不满足
```

在 `compute_pde_residual` 签名中新增参数：

```python
def compute_pde_residual(..., convection_scale: float = 2.0, ...):
```

在 `PDGCN/models/config.py` 中新增配置项：

```python
convection_scale: float = 2.0  # 对流项缩放补偿因子
```

#### 优缺点

| 优点 | 缺点 |
|------|------|
| 一行代码，立即生效 | 因子 2.0 是理论近似值，实际网格需微调 |
| 计算开销零增加 | 对极度非均匀网格可能不够精确 |
| 可配置为可学习参数 | 本质上是"治标不治本" |

---

### 5.2 方案 B（推荐长期方案）：最小二乘梯度重构 + 直接点积

#### 原理

对每个节点 $i$，用最小二乘法从邻居差分重构完整的温度梯度 $\nabla T_i \in \mathbb{R}^3$：

$$
\nabla T_i = \arg\min_{\mathbf{g}} \sum_{j \in \mathcal{N}(i)} \left( \mathbf{g} \cdot \mathbf{e}_{ij} - \frac{T_j - T_i}{d_{ij}} \right)^2
$$

即求解 $3 \times 3$ 正规方程：

$$
(\mathbf{G}^T \mathbf{G}) \nabla T_i = \mathbf{G}^T \mathbf{b}
$$

其中：
- $\mathbf{G} \in \mathbb{R}^{|\mathcal{N}(i)| \times 3}$，每行为 $\mathbf{e}_{ij}^T$
- $\mathbf{b} \in \mathbb{R}^{|\mathcal{N}(i)|}$，每个元素为 $(T_j - T_i) / d_{ij}$

然后直接计算对流项：

$$
R_{conv,i} = \mathbf{v}_i \cdot \nabla T_i
$$

#### 实现伪代码

```python
def convection_least_squares_gradient(T, nodes, edge_index, velocity):
    """通过最小二乘梯度重构 + 直接点积计算对流项"""
    sender, receiver = edge_index
    delta = nodes[receiver] - nodes[sender]
    distance = torch.linalg.norm(delta, dim=-1, keepdim=True)
    e_ij = delta / distance.clamp_min(eps)

    # 预计算伪逆矩阵 (G^T G)^{-1} G^T，与 T 无关，可在图构建时计算
    # 运行时只需矩阵-向量乘法和点积
    ...
    return convection  # [N]
```

#### 优缺点

| 优点 | 缺点 |
|------|------|
| 数学严格，对标 FEM 强形式 | 需要为每个节点求解或预计算 3×3 系统 |
| 不受边方向分布影响 | 预计算伪逆矩阵增加内存开销 |
| 无需任何调参 | 实现复杂度较高 |

---

### 5.3 方案 C（FEM 对标方案）：预装配图微分算子矩阵

借鉴 FEM 的 Galerkin 弱形式，在图上预计算质量和刚度矩阵近似：

- 质量矩阵 $M_{ij} \approx \int N_i N_j d\Omega$
- 对流矩阵 $C_{ij} \approx \int N_i (\mathbf{v} \cdot \nabla N_j) d\Omega$
- 扩散矩阵 $K_{ij} \approx \int \nabla N_i \cdot \nabla N_j d\Omega$

然后使用标准 FEM 装配：$R = M^{-1}(C + K)T$

#### 优缺点

| 优点 | 缺点 |
|------|------|
| 与 FEM 数学上最一致 | 与 GNN 图结构理念差异大 |
| 自动满足紧框架条件 | 实现和维护复杂度最高 |
| 可复用 FEM 代码库的成熟技术 | 大幅偏离当前代码架构 |

---

### 5.4 方案 D（辅助改进）：优化边权重策略

#### D1：等权重替代距离权重

```python
# 替换 alpha = inv_distance / weight_sum
alpha = 1.0 / degree[receiver]  # 等权重
```

#### D2：方向感知权重

设计权重使 $\sum_j \alpha_j \mathbf{e}_j \mathbf{e}_j^T \approx \mathbf{I}$：

```python
# 基于角分布的正规化权重
# 使各方向的二阶矩贡献均衡
```

---

## 6. 建议实施路径

| 优先级 | 阶段 | 行动 | 预期效果 |
|--------|------|------|----------|
| **P0** | 立即 | 方案 A：添加 `convection_scale=2.0` | 立即消除 ~2× 系统性高估 |
| **P1** | 短期 | 将 `convection_scale` 设为可学习参数 | 训练中自动微调最优因子 |
| **P2** | 中期 | 方案 B：最小二乘梯度重构 | 根本上解决非结构化网格逼近问题 |
| **P3** | 中期 | 同步检查扩散项图拉普拉斯精度 | 进一步减少残差偏差 |
| **P4** | 长期 | 方案 C：若精度要求极高，考虑 FEM 式装配 | 对标 FEM 精度 |

---

## 7. 附录：关键公式速查

### FEM 侧（强形式）

$$
\frac{\partial T}{\partial t} + \mathbf{v}_{scan} \cdot \nabla T = \nabla \cdot (\boldsymbol{\alpha} \nabla T) + Q_{source}
$$

### FEM 侧（弱形式，Backward Euler）

$$
\int_\Omega \frac{T^{n+1}}{\Delta t} w \, d\Omega
+ \int_\Omega (\mathbf{v} \cdot \nabla T^{n+1}) w \, d\Omega
+ \int_\Omega (\boldsymbol{\alpha} \nabla T^{n+1}) \cdot \nabla w \, d\Omega
= \int_\Omega \left(\frac{T^n}{\Delta t} + Q\right) w \, d\Omega
$$

### PDGCN 侧（当前图近似）

$$
R_i = \frac{T_i^{n+1} - T_i^n}{\Delta t^*}
+ \underbrace{\sum_{j \in \mathcal{N}(i)} \alpha_{ij} (\mathbf{v}^* \cdot \mathbf{e}_{ij}) \frac{T_i - T_j}{d_{ij}^*}}_{\text{对流：投影乘积近似}}
- \frac{1}{Pe} \underbrace{\sum_{j \in \mathcal{N}(i)} k_{ij} \frac{T_j - T_i}{d_{ij}^{*2}}}_{\text{扩散：图拉普拉斯近似}}
$$

### 紧框架条件（Resolution of Identity）

$$
\sum_{j \in \mathcal{N}(i)} \alpha_{ij} \, \mathbf{e}_{ij} \mathbf{e}_{ij}^T = \mathbf{I}
$$

---

## 8. 变更记录

| 日期 | 版本 | 变更内容 |
|------|------|----------|
| 2026-07-02 | v1.0 | 初始版本：根因分析 + 改进建议 |
