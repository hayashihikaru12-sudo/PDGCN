# 多层推理机制

## 1. 总体思路

本代码库的 PDGCN 网络仅在**单层曲面**上训练，学习曲面内无源输运（对流 + 扩散 + 纤维各向异性导热）。多层推理时，将训练好的单层网络**虚拟复制**到各厚度层，再通过厚度方向的一维隐式 FDM 耦合层间导热，实现多层铺放温度场的预测。

核心思想是**算子分裂（operator splitting）**：在每个时间步内，面内输运和厚度方向导热分别由两个独立模块负责，按顺序施加或交替平均。

## 2. 温度状态表示

多层温度状态张量形状固定为 $[L, N, 1]$，其中：

- $L$：总层数（`num_layers`）
- $N$：每层曲面节点数
- $l=0$ 为顶层（铺放表面），$l=L-1$ 为底层（模具恒温边界）

所有温度均以无量纲形式 $\tilde{T}$ 存储与计算：

$$
\tilde{T} = \frac{T - T_{\text{amb}}}{\Delta T_0}
$$

其中 $T_{\text{amb}}$ 为环境/模具温度，$\Delta T_0$ 为特征温差。

## 3. 单步更新流程

每个时间步的更新遵循以下顺序（默认模式，`use_alternating_order_average=false`）：

### 3.1 第一步：显式表面热源

热源只作用于顶层（$l=0$），读取 HDF5 中的 `dynamic/Q`（表面热流 $q''$），转换为无量纲温升：

$$
\tilde{T}_{l}^{\text{src}} = 
\begin{cases}
\tilde{T}_{l}^{n} + \Delta \tilde{T}_{Q}, & l = 0 \\[4pt]
\tilde{T}_{l}^{n}, & l \geq 1
\end{cases}
$$

施加源项后进行 Dirichlet 边界钳制。

### 3.2 第二步：面内输运（PDGCN）

将训练好的单层 PDGCN 应用到所有 $L$ 层（或仅顶层，由 `pdgcn_inplane_top_layer_only` 控制），计算面内输运增量 $\Delta \tilde{T}_{\text{inplane}}$：

$$
\tilde{T}_{l}^{\text{inplane}} = \tilde{T}_{l}^{\text{src}} + \Delta \tilde{T}_{\text{inplane},l}
$$

#### 3.2.1 多层图的构造

每层图节点特征从单层基础图复制，并根据层索引进行几何变换。

**节点位置偏移**（沿曲面法向）：

$$
\mathbf{x}_l = \mathbf{x}_0 + l \cdot s \cdot \Delta \tilde{z} \cdot \hat{\mathbf{n}}
$$

其中 $s \in \{-1, 1\}$ 为法向偏移方向符号，$\Delta \tilde{z} = \Delta z / L_0$ 为无量纲层间距，$\hat{\mathbf{n}}$ 为曲面单位法向量。

**纤维方向旋转**（Rodrigues 旋转公式，绕法向旋转铺层角 $\theta_l$）：

$$
\hat{\mathbf{f}}_l = \hat{\mathbf{f}}_0 \cos\theta_l + (\hat{\mathbf{n}} \times \hat{\mathbf{f}}_0) \sin\theta_l + \hat{\mathbf{n}} (\hat{\mathbf{n}} \cdot \hat{\mathbf{f}}_0) (1 - \cos\theta_l)
$$

**边索引扩充**：每层内部边拓扑相同，层间无边连接。$L$ 层图的边索引由单层边索引平移得到：

$$
\text{edge\_index}_l = \text{edge\_index}_0 + l \cdot N
$$

**节点特征组装**：每层节点的输入特征为 $[x_l, y_l, z_l, f_{x,l}, f_{y,l}, f_{z,l}, \tilde{T}_l]$，可选追加 $q^*$ 和 $\Delta \tilde{T}_{Q}^*$。

#### 3.2.2 层批量前向

为避免 GPU 显存溢出，多层 PDGCN 前向按**层批量**执行，每次只构造部分层的图，默认 GPU 上每批 ≤4 层。当 OOM 时自动折半重试，直至单层。

#### 3.2.3 Delta 平滑

对面内输运增量 $\Delta \tilde{T}_{\text{inplane}}$ 在图拓扑上按图拉普拉斯平滑，抑制层间不一致的高频噪声：

$$
\Delta \tilde{T}_{\text{smooth}} = (1 - \alpha) \cdot \Delta \tilde{T} + \alpha \cdot \frac{\sum_{j \in \mathcal{N}(i)} \Delta \tilde{T}_j}{|\mathcal{N}(i)|}
$$

迭代 `delta_smoothing_steps` 次，$\alpha$ 为平滑因子（`delta_smoothing_alpha`）。仅对内部节点（非边界节点）更新。

### 3.3 第三步：厚度方向隐式 FDM

在每根曲面法线上独立求解一维瞬态导热（Backward Euler），将各层温度沿厚度方向耦合。

#### 3.3.1 无量纲 FDM 系数

$$
C_n = k_r \cdot \Delta \tilde{t} \cdot \frac{1}{Pe} \cdot \frac{1}{\Delta \tilde{z}^2}
$$

其中 $k_r$ 为厚度向导热系数比（`k_ratio`），$Pe$ 为 Péclet 数倒数之原值（`inverse_pe`），$\Delta \tilde{t}$ 为无量纲时间步长（`dt_star`）。

#### 3.3.2 隐式系统

以底层温度 $\tilde{T}_{\text{bottom}}$ 为 Dirichlet 边界，对活跃层（$l = 0, 1, \ldots, L-2$）构建三对角线性系统：

$$
(\mathbf{I} - C_n \mathbf{D}) \cdot \mathbf{u}^{n+1} = \mathbf{u}^{n}
$$

其中 $\mathbf{u} = \tilde{T} - \tilde{T}_{\text{bottom}}$ 为相对温度（减去底层恒温），三对角矩阵 $\mathbf{I} - C_n \mathbf{D}$ 的结构为：

- 首层（$l=0$）：$1 + C_n$ 在对角线，$-C_n$ 在右邻
- 中间层（$0 < l < L-2$）：$1 + 2C_n$ 在对角线，$-C_n$ 在左右邻
- 末活跃层（$l=L-2$）：$1 + 2C_n$ 在对角线，$-C_n$ 在左邻；底层 $l=L-1$ 被钳制为 $\tilde{T}_{\text{bottom}}$

即对内部活跃层 $l \in [1, L-3]$：

$$
-C_n \cdot u_{l-1}^{n+1} + (1 + 2C_n) \cdot u_{l}^{n+1} - C_n \cdot u_{l+1}^{n+1} = u_{l}^{n}
$$

求解后将相对温度加回底层温度，底层强制恒温：

$$
\tilde{T}_{l}^{n+1} = u_{l}^{n+1} + \tilde{T}_{\text{bottom}} \quad (l < L-1), \qquad \tilde{T}_{L-1}^{n+1} = \tilde{T}_{\text{bottom}}
$$

#### 3.3.3 可选：顶层表面对流热损失

$$
\tilde{T}_{0} \leftarrow \tilde{T}_{\text{bottom}} + \frac{\tilde{T}_{0} - \tilde{T}_{\text{bottom}}}{1 + \gamma \cdot \Delta \tilde{t}}
$$

该项模拟铺放表面与环境的对流散热，$\gamma$ 取决于扫描速度。

#### 3.3.4 可选：瞬态肋片冷却（Transient Fin Cooling）

在隐式系统对角线上叠加冷却项 $\gamma_l \cdot \Delta \tilde{t}$，矩阵对角变为：

$$
\text{diag}_l = 1 + C_{n,\text{up}} + C_{n,\text{down}} + \gamma_l \cdot \Delta \tilde{t}
$$

其中 $\gamma_l^*$ 可由曲率参数 $\beta_H$、特征长度 $r_c^*$ 或直接给定，支持逐层线性/二次型剖面。

### 3.4 边界条件

每步结尾施加 Dirichlet 边界并将底层钳制为恒温：

$$
\tilde{T}_l \big|_{\text{boundary}} = 0, \qquad \tilde{T}_{L-1} = \tilde{T}_{\text{bottom}}
$$

## 4. 交替顺序平均（Strang Splitting 对称化）

当 `use_alternating_order_average=true` 时，每步同时计算两条路径并取平均，以提高算子分裂的对称精度：

**路径 A**：源项 → 面内输运 → 厚度 FDM  
**路径 B**：源项 → 厚度 FDM → 面内输运

$$
\tilde{T}^{n+1} = \frac{1}{2} \left( \tilde{T}_{A}^{n+1} + \tilde{T}_{B}^{n+1} \right)
$$

## 5. 仅顶层 PDGCN 模式

当 `pdgcn_inplane_top_layer_only=true` 时，PDGCN 仅对顶层 $l=0$ 计算面内输运，其余层 $\Delta \tilde{T}_{\text{inplane}} = 0$。这适用于层间热传导远强于面内对流、下层温度主要由厚度 FDM 决定的场景。

## 6. 初始条件

多层温度场初始化为：

- **顶层**（$l=0$）：若提供 `initial_temperature_star` 则直接使用，否则通过 warmup 伪时间松弛获得（若无 warmup 则取图节点初始温度）
- **中间层**（$0 < l < L-1$）：初始化为 $\tilde{T}_{\text{bottom}}$
- **底层**（$l=L-1$）：恒温 $\tilde{T}_{\text{bottom}}$

## 7. 完整更新式总结

单步多层温度更新可总结为：

$$
\boxed{
\tilde{T}^{n+1} = \mathcal{B}\left( \mathcal{F}\left( \mathcal{P}\left( \mathcal{S}\left( \tilde{T}^{n} \right) \right) \right) \right)
}
$$

其中：

| 算子 | 含义 | 作用层 |
|------|------|--------|
| $\mathcal{S}$ | 显式表面热源 | 仅顶层（$l=0$） |
| $\mathcal{P}$ | PDGCN 面内输运 | 全部层或仅顶层 |
| $\mathcal{F}$ | Backward Euler 隐式厚度 FDM | 全部活跃层 |
| $\mathcal{B}$ | Dirichlet 边界 + 底层恒温 | 全部层 |

展开为分量形式（默认顺序）：

$$
\tilde{T}_l^{n+1} = 
\begin{cases}
\text{FDM}_l\left( \tilde{T}_l^{n} + \Delta \tilde{T}_{Q,l} + \Delta \tilde{T}_{\text{PDGCN},l} \right), & l < L-1 \\[6pt]
\tilde{T}_{\text{bottom}}, & l = L-1
\end{cases}
$$

其中 $\Delta \tilde{T}_{Q,l}$ 仅在 $l=0$ 时非零，$\text{FDM}_l$ 为第 3.3 节描述的隐式三对角求解在层 $l$ 的结果。
