# 自适应节点权重方案 

## 1. 目标

解决 PDGCN 在大梯度输入下预测不足（惰性效应），通过对 PDE 残差损失引入基于表面热流的节点自适应权重，使模型更加关注物理上“困难”的高热流区域。

## 2. 核心公式

### 2.1 权重函数

基于无量纲表面热流 $q_i^*$ 构造节点权重：

$$
w_i = w_{\min} + (1 - w_{\min}) \cdot \frac{|q_i^*|}{\max_j |q_j^*| + \varepsilon}
$$

其中 $w_{\min} \in [0, 1]$ 为 $q^* = 0$ 节点的最低权重，推荐起步值 $w_{\min} = 0.2$。

### 2.2 加权 PDE 损失

原损失（内部节点均匀 MSE）：

$$
\mathcal{L}_{\mathrm{pde}} = \frac{1}{|\Omega_{\mathrm{int}}|} \sum_{i \in \Omega_{\mathrm{int}}} \bigl(R_{\mathrm{pde}, i}^*\bigr)^2
$$

改为加权形式：

$$
\mathcal{L}_{\mathrm{pde}} = \sum_{i \in \Omega_{\mathrm{int}}} \tilde{w}_i \cdot \bigl(R_{\mathrm{pde}, i}^*\bigr)^2,
\qquad
\tilde{w}_i = \frac{w_i}{\sum_{j \in \Omega_{\mathrm{int}}} w_j}
$$

权重经归一化且 `detach()` 切断梯度，不参与反向传播。

### 2.3 总损失不变

$$
\mathcal{L}_{\mathrm{total}} = \lambda_{\mathrm{pde}} \mathcal{L}_{\mathrm{pde}} + \lambda_{\mathrm{out}} \mathcal{L}_{\mathrm{outflow}} + \lambda_{\mathrm{smooth}} \mathcal{L}_{\mathrm{smooth}}
$$

仅 $\mathcal{L}_{\mathrm{pde}}$ 的内部统计方式改变，其余项不受影响。
