# PDGCN 神经网络训练结果分析报告

生成日期：2026-06-28

## 1. 报告范围与数据来源

本报告基于当前目录中的既有训练日志、监控图和 FEM/神经网络推理对比结果，对 PDGCN 神经网络的训练收敛性、物理约束表现、峰值温度预测精度和主要风险进行分析。

使用的数据文件如下：

| 类型 | 文件 |
|---|---|
| 训练历史 | `Monitor_plot/history.json` |
| 训练监控图 | `Monitor_plot/figures/training_monitor.png` |
| FEM/NN 单 case 指标 | `FEM_VS_INF/figures/peak_benchmark_metrics.csv` |
| FEM/NN Q,V 分组汇总 | `FEM_VS_INF/figures/peak_benchmark_group_summary.csv` |
| FEM/NN benchmark 图 | `FEM_VS_INF/figures/peak_benchmark_agreement.png` |
| benchmark 脚本配置 | `FEM_VS_INF/peak_benchmark_config.json` |

说明：本报告不重新训练模型，所有数值均从当前文件直接读取或由 CSV/JSON 重新计算得到。

## 2. 关键结论摘要

1. **训练损失显著收敛，但未达到配置阈值。** 当前 `history.json` 记录了 408 个 epoch（epoch 0-407），最终 `loss_total=0.0473`，低于初始值约 7.32 万倍；但配置中的 `loss_threshold=0.005` 尚未达到，因此不应将本次训练描述为“达到阈值早停”。
2. **物理残差项收敛非常充分。** `loss_pde` 从 3452.28 降至 0.00574，`loss_outflow` 从 12.64 降至 3.05e-5，说明 PDE 与出流边界约束在训练目标内已被有效压低。
3. **峰值温度仍是核心短板。** 训练日志中的 case-level 峰值温升 RMSE 最优约 147.16 °C，最终为 153.28 °C；FEM benchmark 中剔除两个异常推理点后，峰值温升 MAE 仍为 143.91 °C，MAPE 为 29.56%。
4. **模型对峰值温升存在稳定低估。** 剔除异常点后，NN 峰值温升约等于 FEM 峰值温升的 70.4%，Pearson 相关系数很高（0.996），但校准偏差大，说明模型学到了趋势，却没有学准幅值。
5. **存在两个严重推理异常点。** 两个低 Q、高 V case 的 NN 峰值分别达到 2478.96 °C 和 2771.22 °C，明显偏离同类样本，是当前 benchmark 统计失真的主要来源之一。

## 3. 训练配置与数据规模

| 项目 | 数值 |
|---|---:|
| 记录 epoch 数 | 408 |
| 配置最大 epoch 数 | 500 |
| 训练总时长 | 76,749 s（约 21.32 h） |
| 单 epoch 平均时长 | 188.11 s |
| 每个 epoch 的窗口数 | 686 |
| batch/window 平均耗时 | 0.2698 s |
| 优化器 | Adam |
| 初始配置学习率 | 1.0e-4 |
| 学习率调度 | `warmup_cosine` |
| warmup epoch | 30 |
| 最小学习率 | 1.0e-7 |
| 梯度裁剪 | 1.0 |
| TBPTT window | 5 |
| 环境温度 `T_amb` | 120 °C |
| 节点输入维度 | 9 |
| 边输入维度 | 7 |
| 全局输入维度 | 1 |
| hidden size | 128 |
| message passing 层数 | 4 |

模型配置显示，本次训练启用了 `include_q_in_features`、`include_delta_t_source_in_features`、`layer_norm` 和自适应 PDE 节点权重；显式温度监督 `supervision.enabled=False`，但峰值监督 `peak_supervision.enabled=True`，`lambda_peak=0.1`，并在前 20 个 epoch 内从 0 线性升至 0.1。

## 4. 训练过程分析

### 4.1 训练监控图

![PDGCN 训练监控](Monitor_plot/figures/training_monitor.png)

图中包含损失收敛、学习率调度、窗口级损失、温度统计和迎风权重动态。总体看，训练早期损失迅速下降，随后进入长尾收敛阶段；最终总损失主要由峰值监督项贡献。

### 4.2 损失收敛

| 指标 | Epoch 0 | Epoch 407 | 最小值所在 epoch | 降低倍数 |
|---|---:|---:|---:|---:|
| `loss_total` | 3464.9203 | 0.04732 | 407 | 7.32e4 |
| `loss_physics` | 3464.9203 | 0.00577 | 407 | 6.01e5 |
| `loss_pde` | 3452.2822 | 0.00574 | 407 | 6.02e5 |
| `loss_outflow` | 12.6380 | 3.05e-5 | 405 | 4.14e5 |
| `loss_peak_temperature_rise` | 53.7738 | 0.41550 | 269 | 1.29e2 |
| `loss_smooth` | 455.4620 | 11.8544 | 1 | 38.42 |

需要特别区分 `loss_smooth` 与总损失目标：从日志数值看，`loss_total` 主要由 `loss_physics + loss_supervised` 组成，`loss_smooth` 更像诊断指标或未加权正则项，并没有直接累加到当前 `loss_total` 中。最终 epoch 中：

| 分量 | 数值 | 占 `loss_total` 比例 |
|---|---:|---:|
| `loss_physics` | 0.00577 | 12.19% |
| `loss_supervised` | 0.04155 | 87.81% |
| `loss_total` | 0.04732 | 100.00% |

这说明后期优化瓶颈已经从 PDE/出流物理残差转移到峰值温升监督。

### 4.3 峰值温度监督表现

| 指标 | 最优值 | 最优 epoch | 最终值 |
|---|---:|---:|---:|
| `case_peak_temperature_rise_rmse` | 147.16 °C | 7 | 153.28 °C |
| `case_peak_temperature_rise_abs_error` | 130.43 °C | 7 | 147.12 °C |
| `peak_temperature_rise_rmse` | 140.86 °C | 79 | 141.74 °C |
| `peak_temperature_rise_abs_error` | 140.86 °C | 79 | 141.74 °C |

峰值相关指标在前 80 个 epoch 内已经接近平台，之后即使 PDE 和 outflow 损失继续下降，峰值误差没有同步改善。这表明当前训练目标对“局部峰值幅值”的约束仍不足，或者网络结构/输入特征对峰值热点的表达能力受限。

### 4.4 学习率调度

| 学习率指标 | 数值 |
|---|---:|
| Epoch 0 | 3.43e-6 |
| 最大值 | 1.00e-4（epoch 29） |
| Epoch 407 | 9.25e-6 |

学习率曲线符合 warmup + cosine decay 形态。训练前期学习率升高期间，损失快速下降；中后期学习率逐步降低，物理损失继续稳定下降，但峰值误差改善有限。因此，单纯延长训练或继续降低学习率，未必能显著解决峰值温度低估问题。

### 4.5 温度统计与物理合理性

| 指标 | Epoch 0 | Epoch 407 | 变化 |
|---|---:|---:|---|
| `temperature_min` | -1226.13 °C | 118.35 °C | 明显修正，接近环境温度 |
| `temperature_max` | 13966.26 °C | 757.89 °C | 极端高温尖峰被压制 |
| `temperature_mean` | 185.50 °C | 165.27 °C | 略有下降 |
| `temperature_var` | 41530.64 | 9815.20 | 明显下降 |

初始 epoch 出现了严重的非物理温度范围，训练后温度下界接近 `T_amb=120 °C`，最高温也回到更合理区间。最终 `temperature_min=118.35 °C`，仍略低于环境温度，说明如果应用场景要求严格满足 `T >= T_amb`，仍应加入显式温度下界约束或输出变换。

### 4.6 迎风权重动态

| 指标 | 初始值 | 最大值 | 最终值 |
|---|---:|---:|---:|
| `gamma_upwind` | 0.8001 | 0.8332（epoch 34） | 0.6479 |
| `gamma_upwind_std` | 9.76e-5 | 0.1482（epoch 294） | 0.1466 |

`gamma_upwind` 从初始的 0.80 降至约 0.65，同时标准差明显增大，说明模型从近似统一的迎风权重转向了更强的空间/窗口自适应调节。这对稳定 PDE 残差有帮助，但也可能强化图网络的平滑倾向，从而压低局部热点峰值。

## 5. FEM 与神经网络推理 benchmark

### 5.1 benchmark 数据概况

| 项目 | 数值 |
|---|---:|
| 有效 HDF5 case | 133 |
| 跳过文件 | 1 |
| 跳过原因 | 缺少 `fem/temperature` |
| FEM 数据集 | `fem/temperature` |
| NN 数据集 | `prediction/pdgcn_single_layer/temperature` |
| 节点数 | 2910 |
| 温度单位 | °C |
| Q 范围 | 1.36991-2.51663 W/mm² |
| V 范围 | 30-60 mm/s |
| Q=0 baseline | 0 个 |

当前 benchmark 没有 Q=0 样本，因此无法评价无热源条件下的 false-rise 行为。

### 5.2 benchmark 主图

![FEM 与神经网络峰值 benchmark](FEM_VS_INF/figures/peak_benchmark_agreement.png)

图 a、b 中有两个 NN 峰值极高的异常点，使全量 R² 和 RMSE 失真；图 c 中 `Q=1.36995, V=60` 重复组也被该异常点拉高。后续统计同时给出全量结果和剔除异常点后的稳健结果。

### 5.3 全量统计与稳健统计

异常点定义：`nn_peak > 1200 °C` 或 `abs_peak_rise_error_pct > 100%`。按此规则识别出 2 个异常推理点。

| 统计口径 | n | FEM 峰值均值 | NN 峰值均值 | Bias | MAE | RMSE | R² | Pearson r | MAPE |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 全量 | 133 | 602.65 | 494.80 | -107.85 | 175.64 | 314.87 | -3.190 | 0.197 | 42.62% |
| 剔除异常点 | 131 | 606.19 | 462.28 | -143.91 | 143.91 | 150.98 | 0.017 | 0.996 | 29.56% |

解释：

- 全量统计中 R² 为 -3.190，主要由两个极端高估点导致。
- 剔除异常点后，Pearson 相关系数达到 0.996，说明模型对 Q,V 趋势排序非常稳定。
- 但剔除异常点后 Bias 为 -143.91 °C，MAPE 为 29.56%，说明主要问题不是随机噪声，而是系统性幅值低估。
- 线性关系近似为 `NN ΔT_peak ≈ 0.706 * FEM ΔT_peak - 1.06 °C`，即 NN 只预测出约 70% 的 FEM 峰值温升。

### 5.4 异常推理点

| 文件 | Q | V | FEM peak | NN peak | NN-FEM | 温升相对误差 | field max abs error |
|---|---:|---:|---:|---:|---:|---:|---:|
| `pre_case_benchmark_Q1p36995_V60_dt0p05_F81_fem.h5` | 1.36995 | 60.0 | 361.70 | 2478.96 | +2117.26 | +875.99% | 2190.94 |
| `pre_case_benchmark_Q1p36999_V56p6667_dt0p05_F84_fem.h5` | 1.36999 | 56.6667 | 380.11 | 2771.22 | +2391.11 | +919.26% | 2456.89 |

这两个样本的 Q 均处于最低热流组附近，正常情况下不应出现 2000 °C 以上的 NN 峰值。建议优先排查这两个 HDF5 文件的推理结果、归一化/反归一化、有效 mask、时间帧对齐和模型输出裁剪逻辑。

### 5.5 按 Q 分组的稳健统计

以下统计剔除了两个异常推理点，并将相近 Q 四舍五入到两位小数后分组。

| Q 组 | n | FEM ΔT 均值 | NN ΔT 均值 | Bias | MAPE |
|---:|---:|---:|---:|---:|---:|
| 1.37 | 18 | 326.63 | 238.03 | -88.60 | 27.11% |
| 1.51 | 21 | 351.85 | 249.21 | -102.64 | 29.32% |
| 1.66 | 12 | 440.27 | 314.79 | -125.48 | 28.81% |
| 1.80 | 17 | 480.29 | 339.86 | -140.42 | 29.50% |
| 1.94 | 9 | 518.69 | 367.94 | -150.74 | 29.34% |
| 2.09 | 7 | 506.94 | 354.23 | -152.71 | 30.23% |
| 2.23 | 19 | 593.60 | 416.65 | -176.95 | 30.13% |
| 2.37 | 14 | 605.62 | 417.83 | -187.79 | 31.23% |
| 2.52 | 14 | 642.88 | 443.47 | -199.42 | 31.19% |

随 Q 增大，绝对误差从约 89 °C 增至约 199 °C；相对误差从约 27% 增至约 31%。这说明模型低估问题在高热流区更严重，且绝对安全裕度风险随热流增强而增加。

### 5.6 按 V 分组的稳健统计

以下统计同样剔除了两个异常推理点。

| V (mm/s) | n | FEM ΔT 均值 | Bias | MAPE |
|---:|---:|---:|---:|---:|
| 30.0 | 14 | 682.68 | -189.87 | 27.72% |
| 33.3 | 13 | 598.85 | -167.91 | 27.76% |
| 36.7 | 14 | 642.97 | -191.06 | 29.59% |
| 40.0 | 14 | 514.35 | -151.82 | 29.31% |
| 43.3 | 14 | 479.18 | -143.61 | 29.75% |
| 46.7 | 12 | 467.73 | -145.84 | 30.98% |
| 50.0 | 15 | 357.60 | -109.44 | 30.56% |
| 53.3 | 14 | 381.10 | -117.93 | 30.71% |
| 56.7 | 10 | 341.75 | -104.20 | 29.99% |
| 60.0 | 11 | 337.04 | -101.39 | 29.36% |

低速度条件下 FEM 峰值温升更高，因此绝对误差也更大；高速条件下绝对误差降低，但相对误差仍在 29%-31% 左右。模型误差更像统一比例低估，而不是只在某个速度区间失效。

### 5.7 相同 Q,V 重复样本

| Q,V 组合 | n | FEM ΔT 均值 | NN ΔT 均值 | Bias | Abs Error % | 备注 |
|---|---:|---:|---:|---:|---:|---|
| Q=1.36995, V=60 | 2 | 244.13 | 1275.33 | +1031.20 | 449.12% | 含一个严重异常点 |
| Q=1.51327, V=50 | 2 | 328.70 | 229.12 | -99.58 | 30.24% | 稳定低估 |
| Q=1.51330, V=40 | 2 | 405.93 | 290.49 | -115.44 | 28.44% | 稳定低估 |
| Q=1.51332, V=60 | 2 | 271.30 | 191.54 | -79.76 | 29.40% | 稳定低估 |
| Q=2.22994, V=60 | 2 | 406.98 | 276.01 | -130.97 | 32.17% | 稳定低估 |
| Q=2.22996, V=46.6667 | 2 | 511.65 | 353.61 | -158.04 | 30.89% | 稳定低估 |
| Q=2.51663, V=43.3333 | 2 | 618.77 | 426.19 | -192.58 | 31.12% | 稳定低估 |

除异常组外，重复样本的误差方向和误差比例都比较一致。这进一步支持“模型校准偏低”这一判断。

## 6. 训练与推理结果的综合诊断

### 6.1 主要矛盾

当前结果的核心矛盾是：**PDE 物理损失已经充分压低，但 FEM 峰值温度仍存在约 30% 的系统性低估。**

这说明模型并不是完全没有学到热传导规律，而是对峰值热点幅值的重建不足。剔除异常点后高 Pearson 相关系数也支持这一点：趋势学得较好，幅值标定不准。

### 6.2 可能原因

| 问题 | 证据 | 影响 |
|---|---|---|
| 峰值监督权重仍不足 | 后期 `loss_total` 已由峰值监督主导，但峰值 RMSE 长期停留在 140-150 °C | 模型难以进一步提升局部峰值幅值 |
| 图卷积消息传递导致热点平滑 | NN ΔT 约为 FEM ΔT 的 70%；峰值低估稳定出现 | 局部极值被邻域平均削弱 |
| 高 Q 区域幅值缩放不足 | Q 组从 1.37 到 2.52 时 Bias 从 -88.60 °C 增至 -199.42 °C | 高热流工况风险最大 |
| 输出缺少物理范围约束 | 最终 `temperature_min=118.35 °C`，略低于 `T_amb` | 无法严格保证温度下界 |
| 推理流程存在异常点 | 两个低 Q case 输出 2400-2700 °C 峰值 | 需要排查归一化、mask 或帧对齐问题 |
| 缺少 Q=0 baseline | benchmark 中 Q=0 样本数为 0 | 无法评价无热源 false-rise |

### 6.3 工程可用性判断

| 应用场景 | 当前模型是否适用 | 原因 |
|---|---|---|
| 快速趋势筛选 | 可谨慎使用 | 剔除异常点后峰值趋势相关性高 |
| 工艺参数相对比较 | 可作为辅助 | Q,V 趋势基本一致，但需校准幅值 |
| 替代 FEM 预测最高温 | 暂不建议 | 峰值系统性低估约 144 °C |
| 安全裕度或烧蚀风险判断 | 不建议直接使用 | 高 Q 区域低估可接近 200 °C |
| 自动闭环优化 | 需先校准和加异常检测 | 当前存在严重推理异常点 |

## 7. 建议的后续改进

1. **先修复两个异常推理点。** 优先复核对应 HDF5 文件中的 `prediction/pdgcn_single_layer/temperature`、`fem/valid_mask`、反归一化参数和时间轴，确认异常来自模型输出还是后处理流程。
2. **增加峰值幅值校准。** 现有稳健关系近似为 `NN ΔT ≈ 0.706 * FEM ΔT`，可先建立线性或分段校准基线；若校准后误差显著下降，说明主问题是幅值缩放而非趋势学习。
3. **强化局部热点监督。** 在已有 top-k peak supervision 基础上，增加峰值附近空间邻域的加权损失、峰值帧定位损失或 hard example mining，尤其提高高 Q case 的采样权重。
4. **加入温度下界和输出范围约束。** 可考虑 `T = T_amb + softplus(...)` 或对低于环境温度的输出增加惩罚，避免非物理冷却值。
5. **补充 Q=0 baseline case。** 当前无法评估 false-rise。建议增加多个 V、路径长度和帧数下的 Q=0 样本。
6. **单独评估异常检测策略。** 在推理端增加峰值范围、温升比例、相邻帧跳变和场最大误差的 sanity check，避免异常结果直接进入工程决策。

## 8. 总结

当前 PDGCN 训练在物理损失层面表现较好：PDE 残差和出流损失均下降 5-6 个数量级，温度范围也从初始的严重非物理状态恢复到较合理区间。但 FEM benchmark 表明，模型尚不能无校准地替代 FEM 进行峰值温度预测。

剔除两个严重异常推理点后，模型对峰值趋势的相关性很强，但峰值温升只有 FEM 的约 70%，形成稳定的系统性低估。下一阶段应优先处理推理异常点，并围绕“峰值幅值校准”和“局部热点保真”改进训练目标与模型输出约束。
