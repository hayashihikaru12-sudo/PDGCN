# PDGCN 神经网络训练结果分析报告

> **生成日期**：2026-06-26  
> **数据来源**：`Monitor_plot/history.json`（训练过程监控数据）与 `FEM_VS_INF/single_layer_batch/*.h5`（FEM 与推理对比基准数据）  
> **配套图表**：`Monitor_plot/figures/training_monitor.png`（训练监控六联图）、`FEM_VS_INF/figures/peak_benchmark_agreement.png`（FEM-NN 峰值对比四联图）

---

## 目录

1. [概述](#1-概述)
2. [模型架构与训练配置](#2-模型架构与训练配置)
3. [训练过程分析](#3-训练过程分析)
   - 3.1 [Loss 收敛分析](#31-loss-收敛分析)
   - 3.2 [学习率调度](#32-学习率调度)
   - 3.3 [温度统计量演变](#33-温度统计量演变)
   - 3.4 [上风权重 (γ_upwind) 动态](#34-上风权重-γ_upwind-动态)
   - 3.5 [窗口损失分布](#35-窗口损失分布)
   - 3.6 [关键里程碑](#36-关键里程碑)
4. [FEM 与推理结果对比](#4-fem-与推理结果对比)
   - 4.1 [数据概览](#41-数据概览)
   - 4.2 [原始峰值温度一致性](#42-原始峰值温度一致性)
   - 4.3 [峰值温升一致性](#43-峰值温升一致性)
   - 4.4 [重复样本稳定性](#44-重复样本稳定性)
   - 4.5 [工艺条件误差分布](#45-工艺条件误差分布)
   - 4.6 [按热流强度 Q 分组的统计](#46-按热流强度-q-分组的统计)
5. [总体结论与建议](#5-总体结论与建议)
6. [附录](#6-附录)

---

## 1. 概述

本报告针对 PDGCN（Physics-Driven Graph Convolutional Network）模型的训练过程及 FEM 基准对比测试进行全面分析。模型目标是通过图神经网络替代传统有限元方法（FEM），实现增材制造过程中温度场的快速推理。

训练在 158 个 epoch 后触发早停（loss_threshold = 0.005），总耗时约 **9.21 小时**。推理基准测试覆盖 **133 组**不同工艺参数（热流 Q、扫描速度 V）组合，与 FEM 参考解进行峰值温度一致性比较。

### 核心发现

| 维度 | 关键结论 |
|------|---------|
| **训练收敛** | Loss 总量下降 ~1,200,000 倍（5922 → 0.005），收敛充分 |
| **PDE 残差** | 最终 PDE loss = 0.0036，达到设定阈值以下 |
| **FEM 峰值对比** | NN 系统性低估峰值温度，平均偏差 -142.5°C |
| **峰值温升 MAPE** | 28.2%（非零热流工况） |
| **趋势一致性** | R² = -0.023，NN 预测不具备优于均值的线性解释力 |
| **误差模式** | 相对误差在 ~28–33% 范围内稳定，呈比例性偏差 |

---

## 2. 模型架构与训练配置

### 2.1 网络结构

| 参数 | 值 |
|------|-----|
| 隐藏层维度 (`hidden_size`) | 128 |
| 消息传递步数 (`message_passing_num`) | 4 |
| 节点输入特征维度 (`node_input_size`) | 9 |
| 边输入特征维度 (`edge_input_size`) | 7 |
| 输出维度 (`output_size`) | 1 |
| 初始上风权重 (`gamma_upwind`) | 0.8 |
| 各向异性门控 (`use_aniso_gate`) | 启用 |
| 层归一化 (`layer_norm`) | 启用 |
| Dropout | 0.0（未启用） |
| 全局特征 (`include_global`) | 启用 |
| 热源特征 (`include_q_in_features`) | 启用 |
| 热源-时间特征 (`include_delta_t_source_in_features`) | 启用 |

### 2.2 物理参数

| 参数 | 值 |
|------|-----|
| λ_pde（PDE 损失权重） | 1.0 |
| λ_outflow（流出边界损失权重） | 1.0 |
| 逆 Peclet 数 (`inverse_pe`) | 0.001005 |
| 热源系数 (`source_coefficient`) | 25.14 |
| 热源吸收率 (`heat_source_absorptivity`) | 1.0 |
| 导热系数比 (`k_ratio`) | 0.12 |
| 无量纲时间步长 (`dt_star`) | 0.0435 |
| 环境温度 (`T_amb`) | 120°C |

### 2.3 训练超参数

| 参数 | 值 |
|------|-----|
| 优化器 | Adam |
| 初始学习率 | 1×10⁻⁴ |
| 最小学习率 | 1×10⁻⁷ |
| 学习率调度策略 | warmup_cosine |
| 预热 epoch 数 | 30 |
| 梯度裁剪 (`grad_clip_norm`) | 未启用（null） |
| TBPTT 窗口 (`tbptt_window`) | 5 |
| 早停阈值 (`loss_threshold`) | 0.005 |
| 最大 epoch 数 | 500 |
| 自适应 PDE 节点权重 | 启用（temperature_continuous_clamped） |
| PDE 节点权重阈值 | 1.0 |

---

## 3. 训练过程分析

### 3.1 Loss 收敛分析

训练监控图的 **子图 a** 展示了各损失分量随 epoch 的演变。

| 损失分量 | 初始值 | 最终值 | 下降倍数 | 分析 |
|----------|--------|--------|----------|------|
| `loss_total` | 5922.29 | 0.0050 | **1,194,052×** | 总损失，触发早停 |
| `loss_pde` | 5713.24 | 0.0036 | **1,599,824×** | PDE 残差，收敛最好 |
| `loss_outflow` | 208.99 | 0.0001 | **1,406,956×** | 流出边界条件，收敛最优 |
| `loss_smooth` | 523.55 | 12.40 | **42×** | 平滑正则项，收敛最慢 |
| `loss_physics` | 5922.29 | 0.0050 | **1,194,052×** | 物理损失（与 total 一致） |
| `loss_beta` | 0.0000 | 0.0000 | — | β 热损失项，始终为零 |

**关键观察**：

1. **Loss 总量呈对数线性下降**：在 epoch 0 到 epoch 10 之间下降最快（从 ~6000 到 ~0.1），随后进入精细调优阶段（epoch 10-157），最终触发 0.005 阈值早停。

2. **PDE loss 与 total loss 高度耦合**：两条曲线几乎重合，说明 PDE 残差是 loss 的主要构成部分。但 PDE loss 在 epoch ~40、~80、~100 附近出现了三次明显的**尖峰波动**（从 ~0.003 跳升至 ~0.01-0.1 量级），这可能是由于：
   - 自适应 PDE 节点权重策略在特定 epoch 调整权重分配
   - 训练数据中某些困难窗口（hard windows）被重新加权
   - 学习率变化引起的优化路径震荡

3. **loss_smooth 是唯一收敛不充分的损失分量**：从 523.6 仅降至 12.4，仅下降 42 倍。最终值（12.4）远高于 loss_threshold（0.005）。但考虑到：
   - loss_total（0.005）在阈值以下，而 loss_smooth（12.4）远高于阈值
   - 说明 loss_smooth 在总损失中的权重系数极小（估计为 ~10⁻⁴ 量级），主要起正则化辅助作用
   - 高平滑损失值可能反映了模型在空间梯度上的数值振荡，这是一个值得关注但优先级较低的优化方向

4. **Early stopping 行为合理**：在 epoch 157 触发 loss_threshold 早停，loss_total 已连续多 epoch 稳定在 0.005 附近，表明模型已充分收敛。

### 3.2 学习率调度

训练监控图的 **子图 b** 展示了学习率变化。

| 阶段 | Epoch | LR 范围 | 描述 |
|------|-------|---------|------|
| 预热期 | 0–29 | 3.43×10⁻⁶ → 1.0×10⁻⁴ | 线性预热 |
| 峰值平台 | 29–35 | 1.0×10⁻⁴ | 平稳期 |
| 余弦衰减 | 35–157 | 1.0×10⁻⁴ → 8.28×10⁻⁵ | 余弦退火 |

学习率从 3.43×10⁻⁶ 线性预热至峰值 1.0×10⁻⁴（epoch 29），随后采用余弦衰减，最终降至 8.28×10⁻⁵。衰减幅度较小（仅 ~17%），与 loss 在后期仍缓慢下降的特征相符。余弦调度在 epoch 100 后趋于平缓，有助于在收敛尾段保持稳定。

### 3.3 温度统计量演变

训练监控图的 **子图 e** 展示了模型输出温度的统计量演变。

| Epoch | T_max (°C) | T_mean (°C) | T_min (°C) | T_var |
|-------|------------|-------------|------------|-------|
| 0 | **17926.27** | 203.87 | **-2783.09** | 43630.27 |
| 1 | 759.96 | 155.80 | -83.33 | 2548.23 |
| 10 | 724.56 | 164.76 | 103.21 | 9558.87 |
| 50 | 719.57 | 164.85 | 106.38 | 9691.96 |
| 100 | 717.72 | 164.61 | 93.23 | 9566.31 |
| 157 | 717.79 | 164.94 | 114.40 | 9701.66 |

**关键观察**：

1. **Epoch 0 温度严重异常**：T_max 高达 ~17927°C，T_min 低至 -2783°C，这是随机初始化网络的典型表现。仅 1 个 epoch 后便恢复到合理范围（T_max=760°C，T_min=-83°C），表明物理驱动（PDE loss）的约束极强。

2. **快速稳定**：从 epoch 1 开始，T_max 稳定在 ~720°C 附近，T_mean 在 ~165°C 附近，T_min 在 ~100°C 附近。后续 156 个 epoch 中几乎没有发生实质性变化。

3. **T_min 偶现负值**：直至 epoch 10 后 T_min 才稳定在正温度范围（>100°C），但在 epoch 100 仍有降至 93°C 的情况。这表明模型在某些区域的低温预测仍存在轻微不稳定性。

4. **温度方差较大**：T_var 在 9500–9700 范围内，说明温度场空间分布不均匀性较大（符合物理预期——热源附近温度高，远端温度低）。

### 3.4 上风权重 (γ_upwind) 动态

训练监控图的 **子图 f** 展示了可学习的上风权重参数演变。

| Epoch | γ_upwind | γ_upwind_std |
|-------|----------|-------------|
| 0 | 0.7998 | 0.0005 |
| 5 | 0.8062 | 0.0019 |
| 10 | 0.8173 | 0.0024 |
| 50 | 0.7925 | 0.1188 |
| 100 | 0.6850 | 0.2126 |
| 157 | **0.6532** | **0.2283** |

**关键观察**：

1. **γ_upwind 呈下降趋势**：初始值 0.80 在预热期小幅上升至 0.817，随后单调下降至 0.653。γ 控制对流项中上风方向与中心差分的混合比例（γ=1 为纯上风，γ=0 为中心差分）。γ 的下降意味着模型逐渐减少数值耗散（numerical diffusion），倾向于更精确的中心差分。

2. **方差持续增大**：γ_std 从 0.0005 增至 0.2283，说明不同节点的上风权重分化越来越大。这反映了模型在学习"不同区域需要不同数值耗散量"的策略——高梯度区域（如热源附近）可能需要更多上风权重来稳定，而平滑区域可以减少上风权重以提高精度。

3. **γ 下降与系统性低估的关联假设**：γ_upwind 的下降（减少数值耗散）理论上有助于提高温度峰值精度。但从 FEM-NN 对比结果来看，NN 仍系统性低估峰值温度 ~28%。这说明**仅靠调整上风权重不足以消除偏差**，可能还存在其他机制（如等效扩散系数偏高、边界条件处理偏差等）。

### 3.5 窗口损失分布

训练监控图的 **子图 c（热力图）** 和 **子图 d（分位数图）** 展示了 2892 个训练窗口的损失分布。

| 指标 | Epoch 0 | Epoch 157 |
|------|---------|-----------|
| 5th 百分位 | 24.01 | 0.0011 |
| 中位数 | 38.52 | 0.0033 |
| 95th 百分位 | 9051.96 | 0.0140 |
| 低于 0.005 阈值比例 | — | **70.2%**（2030/2892） |
| 低于 0.1 比例 | — | **100%**（2892/2892） |

**关键观察**：

1. **多数窗口已收敛到阈值以下**：70.2% 的窗口损失低于 0.005 阈值，100% 低于 0.1，表明绝大部分训练窗口已充分学习。

2. **热力图中的异常结构**：
   - Epoch 80 附近出现一条明显的**横向亮带**（高损失带），与 PDE loss 的尖峰波动时间点吻合
   - 部分特定窗口（约 Training Window 1500 和 2500 附近）呈现**垂直亮条纹**，暗示这些窗口在所有 epoch 中都相对更难以学习，可能是复杂几何区域或高梯度区域

3. **分布收窄**：窗口损失分布从初始的极度分散（5%-95% 范围跨越 3 个量级）收窄至高度集中（5%-95% 仅在 0.001-0.014 范围内），表明模型对不同工况的拟合质量趋于一致。

### 3.6 关键里程碑

| 里程碑 | 达到 Epoch | Loss 值 |
|--------|-----------|---------|
| loss_total < 100 | 1 | 9.08 |
| loss_total < 10 | 1 | 9.08 |
| loss_total < 1 | 4 | 0.82 |
| loss_total < 0.1 | 10 | 0.096 |
| loss_total < 0.01 | **102** | 0.0098 |
| 触发早停（<0.005） | 157 | 0.0050 |

训练前期收敛极快：第一个 epoch 便将 loss 降低了 3 个数量级。但从 0.01 到 0.005 的"最后一公里"消耗了 55 个 epoch（102→157），占总训练时间的 ~35%。这反映了物理约束在高精度要求下的收敛难度。

---

## 4. FEM 与推理结果对比

### 4.1 数据概览

| 项目 | 数值 |
|------|------|
| 处理 HDF5 文件数 | 133 |
| 跳过无效文件数 | 1（缺失 `fem/temperature` 数据集） |
| 节点数 | 2910 |
| 温度单位 | °C |
| 环境温度 T_amb | 120°C |
| 工艺参数 Q 范围 | 1.37–2.52 W/mm² |
| 工艺参数 V 范围 | 30–60 mm/s |
| FEM 峰值范围 | 361–1005°C |
| NN 峰值范围 | 329–708°C |

### 4.2 原始峰值温度一致性

训练对比图的 **子图 a** 比较 FEM 与 NN 的原始峰值温度。

| 指标 | 数值 |
|------|------|
| FEM 均值 | 602.65°C |
| NN 均值 | 460.18°C |
| Bias (NN−FEM) | **−142.47°C** |
| MAE | 142.47°C |
| RMSE | 155.59°C |
| R² | **−0.023** |
| Paired t-test p | <0.001 |

**分析**：

- 所有 133 个测试 case 中，NN 的峰值温度**无一例外**低于 FEM。散点全部落在 y=x 对角线的下方。
- R² = −0.023（负值）意味着 NN 预测甚至不如使用 FEM 均值（602.65°C）作为常数预测器。但这并非说明 NN 完全失效——从散点图可见，NN 输出仍随 FEM 增加呈上升趋势，只是存在**强烈的系统性比例偏差**（详见 4.3）。
- Bias ≈ MAE，表明偏差的方差极小——几乎所有误差都是系统性的单向偏差，几乎没有随机散射。

### 4.3 峰值温升一致性

去除环境温度（120°C）影响后，训练对比图的 **子图 b** 比较 ΔT_peak = T_peak − T_amb。

| 指标 | 数值 |
|------|------|
| FEM ΔT 均值 | 482.65°C |
| NN ΔT 均值 | 340.18°C |
| Bias (NN−FEM) | **−142.47°C** |
| MAE | 142.47°C |
| RMSE | 155.59°C |
| R² | −0.023 |
| **MAPE** | **28.23%** |
| 误差方向 | **系统性低估** |
| Q=0 baseline case | **0 个**（数据集中无 Q=0 case） |

**分析**：

1. **相对误差比例稳定**：虽然绝对偏差从 ~32°C（低 Q/V）到 ~297°C（高 Q/V）不等，但相对误差（MAPE）集中在 **28.2%** 左右。逐 case 观察，signed peak-rise error% 在 −13% 到 −35% 范围内，均值约 −28%。这说明 NN 预测 ≈ FEM × 0.72 + T_amb × 0.28，呈现明确的**比例性低估**模式。

2. **误差来源假设**：
   - **等效热扩散率偏高**：模型中的有效导热系数或数值耗散高于 FEM，导致热量扩散过快，峰值温升被压低
   - **上风权重 (γ)** 的残余耗散：虽然 γ 从 0.80 降至 0.65，但 0.65 的 γ 仍有显著的迎风耗散效应
   - **时间离散格式**：`residual_time_scheme` 和 `dt_star=0.0435` 的无量纲时间步长可能导致时间方向上的额外数值扩散
   - **边界条件处理**：`dirichlet_temperature_star=0.0` 和 `thermal_loss_base_temperature_star=0.0` 可能未完全匹配 FEM 的边界条件设定

3. **无 Q=0 baseline**：数据集中不含 Q=0（无热源）case，因此无法评估模型在无热源条件下的 baseline false-rise 行为。建议在后续测试中补充 Q=0 case。

### 4.4 重复样本稳定性

训练对比图的 **子图 c** 展示了存在重复样本的 Q,V 组合。共有 **7 组**重复 Q,V 组合（每组 2 个 case），用于检验相同工艺条件下模型误差的稳定性。

| Q (W/mm²) | V (mm/s) | n | FEM ΔT 均值 | NN ΔT 均值 | ΔT Bias | Abs Error % |
|-----------|----------|---|------------|------------|----------|-------------|
| 1.370 | 60 | 2 | 244.13 | 209.99 | −34.14 | 13.98% |
| 1.513 | 50 | 2 | 328.70 | 247.62 | −81.08 | 24.58% |
| 1.513 | 40 | 2 | 405.93 | 298.77 | −107.15 | 26.40% |
| 1.513 | 60 | 2 | 271.30 | 221.25 | −50.05 | 18.45% |
| 2.230 | 60 | 2 | 406.98 | 277.22 | −129.75 | 31.87% |
| 2.230 | 46.67 | 2 | 511.65 | 346.29 | −165.36 | 32.32% |
| 2.517 | 43.33 | 2 | 618.77 | 414.35 | −204.42 | 33.04% |

**分析**：

1. **误差在重复样本间高度一致**：同一 Q,V 组合下的两个 case，NN 输出几乎完全相同（差异通常 <1°C），说明**模型推理具有极高的确定性**（无显著随机波动）。

2. **相对误差随 Q 增大而增大**：低 Q（1.37, V=60）的误差仅 14%，而高 Q（2.52, V=43.33）的误差达 33%。这符合"比例性偏差"假说——偏差随温度水平等比例增长。

3. **每个分组内 NN 始终低于 FEM**：7 组重复实验中，没有一组出现 NN 高估 FEM 的情况，偏差方向完全一致。

### 4.5 工艺条件误差分布

训练对比图的 **子图 d** 按 Q,V 工艺条件展示 signed peak-rise relative error。

**关键发现**：

- **全局蓝色**：图中所有点均呈蓝色（负误差），无红色（正误差）点出现。这从全局视角确认了系统性低估的结论。
- **V 的影响较弱**：在同一 Q 水平下，不同 V 值的误差百分比差异不大，说明扫描速度对误差比例的影响较小。
- **Q 的影响较强**：误差百分比随 Q 增大呈上升趋势。低 Q（~1.37）区域误差约 13–25%，高 Q（~2.52）区域误差约 32–34%。
- **无 Q=0 点**：图中无 Q=0 基底行（通常显示在 Q=0 或 Q 轴下方），无法评估 baseline 行为。

### 4.6 按热流强度 Q 分组的统计

| Q (W/mm²) | n | FEM ΔT 均值 | NN ΔT 均值 | ΔT Bias | ΔT MAE | Abs Error % |
|-----------|---|------------|------------|----------|--------|-------------|
| 1.37 | 19 | 324.68 | 256.02 | −68.66 | 68.66 | 21.07% |
| 1.51 | 16 | 365.95 | 277.54 | −88.41 | 88.41 | 24.09% |
| 1.66 | 10 | 424.01 | 311.22 | −112.79 | 112.79 | 27.00% |
| 1.80 | 12 | 465.24 | 338.02 | −127.22 | 127.22 | 28.70% |
| 1.94 | 8 | 500.57 | 355.79 | −144.78 | 144.78 | 30.18% |
| 2.09 | 7 | 511.28 | 356.53 | −154.74 | 154.74 | 30.86% |
| 2.23 | 12 | 591.84 | 407.15 | −184.69 | 184.69 | 31.88% |
| 2.37 | 10 | 591.44 | 401.66 | −189.78 | 189.78 | 32.49% |
| 2.52 | 11 | 662.92 | 446.21 | −216.71 | 216.71 | 33.31% |

**规律总结**：绝对误差和相对误差均随 Q 单调递增。相对误差从 Q=1.37 的 21.1% 增长至 Q=2.52 的 33.3%，增速逐渐放缓。这种单调关系进一步支持了"比例性偏差"的解释框架。

---

## 5. 总体结论与建议

### 5.1 训练收敛评估 ✅

模型训练过程**技术上是成功的**：

- PDE 残差从 ~5700 降至 0.0036，下降 6 个数量级
- 总损失在 157 个 epoch 内触发 0.005 阈值早停
- 温度场从初始混乱状态（T_max=17927°C, T_min=−2783°C）快速稳定至物理合理范围
- 2892 个训练窗口中 70.2% 已达到 loss < 0.005，100% 低于 0.1
- 上风权重 γ 自主学习调整至 0.653，方差 0.228

### 5.2 FEM-NN 一致性评估 ⚠️

模型在与 FEM 的**峰值温度对比中存在显著系统性偏差**：

| 严重程度 | 指标 | 评估 |
|----------|------|------|
| 🔴 **严重** | R² = −0.023 | 不具备面向 FEM 的线性预测力 |
| 🔴 **严重** | Bias = −142.5°C | 系统性低估，方向一致 |
| 🟡 **需关注** | MAPE = 28.2% | 相对误差稳定但偏高 |
| 🟢 **可用** | 误差一致性 | 偏差比例稳定，可标定校正 |

### 5.3 偏差原因分析

系统性 ~28% 的低估最可能由以下因素共同导致：

1. **数值耗散（Numerical Diffusion）**：γ_upwind 收敛至 0.65，仍引入显著的上风耗散，导致温度峰值的数值"抹平"。这是 PINN/GCN 类方法处理对流主导问题时的已知挑战。

2. **等效扩散系数偏高**：`inverse_pe = 0.001` 对应 Pe ≈ 1000，结合 `k_ratio = 0.12`，有效导热可能高于 FEM 的设定值。

3. **时间离散误差**：TBPTT 窗口=5 和 dt_star=0.0435 的组合可能引入时间方向的累积离散误差。

4. **边界条件不匹配**：`dirichlet_temperature_star=0.0` 和 `thermal_loss_base_temperature_star=0.0` 可能与 FEM 的边界处理存在差异。

5. **训练-推理分布偏移**：训练监控中 `fem_temperature_mae` 等指标始终为 0，提示训练过程中可能未对 FEM 参考解进行直接监督（训练目标仅为 PDE 残差 + 边界条件），而非数据驱动的监督学习。这解释了为何 PDE loss 极低而 FEM 对比偏差很大的现象——**模型学到的是一组 PDE 的解，但与 FEM 的特定数值解存在系统性偏移**。

### 5.4 改进建议

#### 短期（可快速验证）

| 优先级 | 建议 | 预期效果 |
|--------|------|---------|
| 🔴 P0 | **补充 Q=0 baseline 测试 case** | 评估无热源条件下的 baseline false-rise |
| 🔴 P0 | **对推理输出施加线性标定校正** | 利用已知的 ~28% 比例偏差进行后处理校正，验证标定后的一致性 |
| 🟡 P1 | **检查 inverse_pe 参数** | 尝试降低 inverse_pe（提高 Pe 数），减少等效扩散 |
| 🟡 P1 | **对比不同 γ_upwind 固定值的推理结果** | 明确 γ 对峰值低估的定量贡献 |

#### 中期（需要重新训练）

| 优先级 | 建议 | 预期效果 |
|--------|------|---------|
| 🟡 P1 | **在训练 loss 中增加 FEM 峰值监督项** | 直接约束峰值温度一致性，可能显著改善 R² |
| 🟡 P1 | **尝试 γ_upwind=0.5 或更低的初始值** | 减少上风耗散，提高峰值精度（需注意数值稳定性） |
| 🟢 P2 | **增加时间分辨率（减小 dt_star）** | 减少时间离散误差 |
| 🟢 P2 | **引入高阶空间离散格式** | 替代一阶上风格式，减少数值耗散 |

#### 长期（方法论层面）

| 优先级 | 建议 |
|--------|------|
| 🟢 P2 | 采用 **Operator Learning**（如 DeepONet、FNO）等架构，直接学习 FEM 解算子而非物理约束 |
| 🟢 P2 | 引入 **多 fidelity 数据融合**：结合少量高精度 FEM 数据和大量低精度 PINN 推理 |
| 🟢 P2 | 进行**敏感性分析**，量化 each 物理参数对峰值预测的影响 |

### 5.5 当前模型适用场景

基于以上分析，当前 PDGCN 模型：

- ✅ **适用**：工艺参数趋势筛选、参数敏感性分析、定性比较
- ⚠️ **谨慎适用**（经标定校正后）：快速的相对误差评估（如"工艺A比工艺B高约X%"的判断）
- ❌ **不推荐直接使用**：安全裕度评估、最高温度合规判断、替代 FEM 进行精确峰值预测

---

## 6. 附录

### A. 配套图表清单

| 图表 | 路径 |
|------|------|
| 训练监控六联图 | `Monitor_plot/figures/training_monitor.png` |
| 训练监控图（SVG） | `Monitor_plot/figures/training_monitor.svg` |
| 训练监控图（PDF） | `Monitor_plot/figures/training_monitor.pdf` |
| FEM-NN 峰值对比四联图 | `FEM_VS_INF/figures/peak_benchmark_agreement.png` |
| FEM-NN 峰值对比图（SVG） | `FEM_VS_INF/figures/peak_benchmark_agreement.svg` |
| FEM-NN 峰值对比图（PDF） | `FEM_VS_INF/figures/peak_benchmark_agreement.pdf` |

### B. 数据文件清单

| 文件 | 说明 |
|------|------|
| `Monitor_plot/history.json` | 158 epoch 的完整训练历史（含 loss、lr、温度、γ 等全部指标） |
| `FEM_VS_INF/figures/peak_benchmark_metrics.csv` | 133 case 的逐 case FEM-NN 峰值对比指标 |
| `FEM_VS_INF/figures/peak_benchmark_group_summary.csv` | 按 Q,V 分组的汇总统计 |
| `FEM_VS_INF/figures/peak_benchmark_skipped_files.csv` | 跳过的无效文件及原因 |
| `FEM_VS_INF/single_layer_batch/*.h5` | 原始 HDF5 数据（FEM 与 NN 温度场） |

### C. 模型关键公式

模型的核心物理约束为对流-扩散-反应方程（无量纲化）：

$$\frac{\partial T^*}{\partial t^*} + \mathbf{v}^* \cdot \nabla T^* = \frac{1}{\text{Pe}} \nabla^2 T^* + S^*$$

其中：
- $T^* = T / T_{\text{ref}}$（无量纲温度）
- $\text{Pe}$ 为 Peclet 数（`inverse_pe = 1/Pe = 0.001`）
- $S^*$ 为无量纲热源项（由 `source_coefficient × Q × heat_source_absorptivity` 决定）

上风权重 γ（`gamma_upwind`）控制对流项的离散：对流项 = γ × (一阶上风) + (1−γ) × (中心差分)，γ=1 为纯上风（最大耗散），γ=0 为中心差分（无耗散、可能振荡）。

---

> **报告编写**：基于 `Monitor_plot/history.json` 和 `FEM_VS_INF/plot_peak_benchmark.py` 的自动分析输出  
> **图表示例**：由 `plot_training_monitor.py`（训练监控）和 `plot_peak_benchmark.py`（基准对比）生成  
> **数据截止**：2026-06-26
