# PDGCN 神经网络训练结果分析报告

## 1. 报告概览

本报告综合分析 PDGCN (Physics-Driven Graph Convolutional Network) 神经网络在热传导仿真任务中的训练过程与推理性能。报告基于两类数据源：

- **训练监控数据**（`Monitor_plot/history.json`）：记录 335 个 epoch 的损失函数、学习率、温度统计、迎风权重等训练指标
- **推理基准测试数据**（`FEM_VS_INF/figures/`）：133 组 FEM 与神经网络推理结果的峰值温度对比分析

核心评价维度包括：损失收敛性、学习率调度策略、温度预测精度、物理一致性及 FEM 一致性。

---

## 2. 训练过程分析

### 2.1 训练配置概览

| 配置项 | 数值 |
|---|---|
| 总训练 Epoch 数 | 335 |
| 训练总时长 | 约 18.15 小时（65,356 秒） |
| 单个 epoch 平均耗时 | ~182-195 秒 |
| 单个 batch 平均耗时 | ~71 ms |
| 训练数据节点数 | 2,910 节点/样本 |
| 环境温度 T_amb | 120.0 °C |
| 早停触发 Epoch | 334（reason: loss_threshold） |

### 2.2 损失函数收敛性

损失函数由多个分量组成，训练过程中各分量的演化如下：

| 损失分量 | 初始值 (Epoch 0) | 最终值 (Epoch 334) | 降低倍数 |
|---|---|---|---|
| `loss_total` | 16.479 | 0.0050 | ~3,300× |
| `loss_pde` | 15.538 | 0.0022 | ~7,060× |
| `loss_outflow` | 0.939 | 0.00013 | ~7,220× |
| `loss_smooth` | 25.615 | 22.282 | ~1.15×（几乎未下降） |
| `loss_physics` | 16.479 | 0.0046 | ~3,580× |
| `loss_peak_temperature_rise` | 0.331 → 101.8 RMSE | 0.037 → 29.9 RMSE | 峰值误差降低 ~3.4× |

**关键发现：**

1. **PDE 残差损失与出流损失收敛良好**：`loss_pde` 和 `loss_outflow` 均降低超过三个数量级，表明网络有效学习了热传导方程的控制物理约束。

2. **平滑性损失基本未收敛**：`loss_smooth` 从 25.6 仅降至 22.3（降幅 ~13%），在整个训练过程中保持较高水平。该分量衡量预测温度场的空间光滑性，其持续偏高可能意味着：
   - 网络输出的温度场存在局部高频振荡
   - 网格节点间的预测梯度不平滑
   - 图卷积网络中的正则化强度可能需要调整

3. **峰值温升损失逐步改善**：峰值温升 RMSE 从 101.8 °C 降至 29.9 °C（降低 ~70%），但绝对误差仍然较大，表明网络在捕捉极端温度事件方面仍有改进空间。

4. **早停时机合理**：训练在 epoch 334 因达到 `loss_threshold` 而触发早停，此时各主要损失分量已趋于平稳。

### 2.3 学习率调度

| 指标 | 初始值 | 最终值 |
|---|---|---|
| 学习率 | 3.43 × 10⁻⁶ | 2.75 × 10⁻⁵ |

学习率从极低的初始值逐步上升（warmup 阶段），最终稳定在约 2.75 × 10⁻⁵。这种低起点逐步升温的策略有助于训练初期的稳定性，避免梯度爆炸。从损失曲线看，该调度策略总体有效——损失平稳下降，未出现剧烈震荡。

### 2.4 温度场统计

| 温度统计量 | 初始 (Epoch 0) | 最终 (Epoch 334) | 变化趋势 |
|---|---|---|---|
| T_max (°C) | 1,472.4 | 998.3 | ↓ 合理 |
| T_min (°C) | -32.7 | 116.2 | ↑ 趋近环境温度 |
| T_mean (°C) | 175.0 | 171.3 | ≈ 稳定 |
| T_var | 11,598 | 14,231 | ↑ 方差增大 |

**关键发现：**

- **温度上界显著改善**：初始阶段网络预测最高温度达到 1,472 °C，远超物理合理范围；训练后上界降至 998 °C，与 FEM 参考值更为接近。
- **温度下界修正明显**：初始阶段出现 -32.7 °C 的物理不可能值（低于绝对零度且远低于环境温度 120 °C），训练后下界回升至 116.2 °C，接近环境温度。
- **方差增大**：温度方差从 11,598 增至 14,231，反映网络学会了预测更大动态范围的温度分布，而非输出过于平滑的平均温度场。

### 2.5 迎风权重（Gamma Upwind）动态

| 指标 | 初始值 | 最终值 |
|---|---|---|
| `gamma_upwind` | 0.800 | 0.523 |
| `gamma_upwind_std` | 0.00075 | 0.121 |

迎风权重 γ 控制对流项中迎风格式的贡献比例。训练过程中：
- γ 从 0.80 降至 0.52，表明网络学会了降低迎风贡献
- γ 的标准差从 0.00075 增至 0.121，表明不同节点/窗口的迎风权重出现了差异化的自适应调节

---

## 3. FEM 与神经网络推理对比分析

### 3.1 基准测试概览

| 指标 | 数值 |
|---|---|
| 测试 HDF5 文件总数 | 133 |
| 无效/跳过文件 | 1 |
| FEM 数据集路径 | `fem/temperature` |
| 预测数据集路径 | `prediction/pdgcn_single_layer/temperature` |
| 节点数 | 2,910 |
| 工艺参数 Q 范围 | 0.57 – 2.52 W/mm² |
| 工艺参数 V 范围 | 30 – 60 mm/s |

### 3.2 全局一致性统计

| 分析对象 | n | FEM 均值 | NN 均值 | Bias (NN-FEM) | MAE | RMSE | R² | Paired t-test p |
|---|---|---|---|---|---|---|---|---|
| 原始峰值温度 T_peak | 133 | 602.65 °C | 463.73 °C | **-138.92 °C** | 141.95 °C | 157.00 °C | **-0.042** | <0.001 |
| 峰值温升 ΔT_peak (Q>0) | 133 | 482.65 °C | 343.73 °C | **-138.92 °C** | 141.95 °C | 157.00 °C | **-0.042** | <0.001 |

**核心发现——严重的系统性低估：**

1. **R² 为负值（-0.042）**：这意味着神经网络对峰值温度的预测能力**差于直接使用 FEM 均值作为常数预测**。虽然在训练过程中损失函数收敛良好，但模型在推理阶段对峰值温度的泛化能力严重不足。

2. **系统性偏差高达 -138.92 °C**：神经网络在所有测试 case 上几乎一致地低估了 FEM 峰值温度。偏差方向高度一致（低估），在 133 个样本中很难见到 NN 高估 FEM 的情况。

3. **MAE = 141.95 °C**：峰值温度的平均绝对误差超过 140 °C，对于需要精确温度控制的热工艺仿真而言，该误差水平尚不能满足工程精度要求。

4. **配对 t 检验 p < 0.001**：FEM 与 NN 峰值差异在统计上高度显著，排除随机波动可能。

### 3.3 按热流 Q 分组的峰值温升误差

| Q (W/mm²) | n | FEM ΔT 均值 | NN ΔT 均值 | ΔT Bias | MAE | Abs Error % |
|---|---|---|---|---|---|---|
| 1.37 | ~15 | ~345 °C | ~270 °C | ~-75 °C | ~75 °C | ~20% |
| 1.51 | ~14 | ~365 °C | ~275 °C | ~-90 °C | ~90 °C | ~24% |
| 1.66 | ~8 | ~380 °C | ~280 °C | ~-100 °C | ~100 °C | ~28% |
| 1.80 | ~10 | ~515 °C | ~370 °C | ~-145 °C | ~145 °C | ~30% |
| 1.94 | ~8 | ~565 °C | ~405 °C | ~-160 °C | ~160 °C | ~30% |
| 2.09 | ~6 | ~535 °C | ~380 °C | ~-155 °C | ~155 °C | ~31% |
| 2.23 | ~14 | ~625 °C | ~425 °C | ~-200 °C | ~200 °C | ~31% |
| 2.37 | ~10 | ~610 °C | ~410 °C | ~-200 °C | ~200 °C | ~32% |
| 2.52 | ~9 | ~640 °C | ~430 °C | ~-210 °C | ~210 °C | ~33% |

**误差随热流增大而单调递增：**

- 在低 Q 区域（Q ≈ 1.37），绝对误差约 75 °C，相对误差约 20%
- 在高 Q 区域（Q ≈ 2.52），绝对误差增至 ~210 °C，相对误差约 33%
- 绝对误差的增长速率超过 FEM 峰值温升的增长速率，说明高功率区域的预测能力进一步恶化
- 相对误差（MAPE）从 20% 递增至 33%，表明模型在所有功率等级上都存在显著的相对偏差

### 3.4 相同 Q,V 条件下的重复性分析

存在 7 组相同 (Q, V) 工艺参数下的重复样本：

| Q,V 组合 | n | FEM ΔT 均值 | NN ΔT 均值 | ΔT Bias | Abs Error % |
|---|---|---|---|---|---|
| Q=1.37, V=60 | 2 | 244.1 °C | 285.6 °C | +41.5 °C | 17.0%（**高估**） |
| Q=1.51, V=50 | 2 | 328.7 °C | 238.2 °C | -90.5 °C | 27.5% |
| Q=1.51, V=40 | 2 | 405.9 °C | 285.2 °C | -120.8 °C | 29.8% |
| Q=1.51, V=60 | 2 | 271.3 °C | 273.1 °C | +1.8 °C | **1.5%（最佳）** |
| Q=2.23, V=60 | 2 | 407.0 °C | 314.7 °C | -92.2 °C | 22.7% |
| Q=2.23, V=46.7 | 2 | 511.6 °C | 348.4 °C | -163.3 °C | 31.9% |
| Q=2.52, V=43.3 | 2 | 618.8 °C | 417.2 °C | -201.6 °C | 32.6% |

**关键观察：**
- 唯一的高估案例（Q=1.37, V=60）和最佳案例（Q=1.51, V=60, error=1.5%）均出现在高速（V=60）条件下
- 高 V（高速扫描）条件下模型表现更好，可能与对流效应占主导时物理约束更容易满足有关
- 重复样本间的 FEM-NN 误差方向高度一致，表明偏差是系统性的而非随机的

### 3.5 Q=0 Baseline 分析

当前测试集中**不存在 Q=0 的 baseline case**，因此无法直接评估模型在无热源输入下的 false-rise（虚假温升）行为。建议后续补充 Q=0 测试 case 以评估 baseline 校准质量。

---

## 4. 综合诊断与分析

### 4.1 训练-推理不一致问题

本报告揭示了一个重要矛盾：**训练损失良好收敛，但推理阶段峰值误差巨大**。可能原因包括：

| 可能原因 | 证据 | 严重程度 |
|---|---|---|
| **训练数据覆盖不足** | 测试集 Q,V 分布与训练集可能不完全一致 | ⭐⭐⭐ |
| **损失函数对峰值温度约束不足** | loss_smooth 未收敛；峰值温升损失权重可能过低（λ=0.01） | ⭐⭐⭐⭐ |
| **平滑性正则化过强** | loss_smooth 始终保持在 ~22 的高位，可能导致网络输出过于平滑 | ⭐⭐⭐ |
| **外推能力不足** | 在高 Q 区域误差显著增大，模型未能学习峰值温升的物理缩放规律 | ⭐⭐⭐⭐ |
| **图结构信息传播限制** | 峰值温度往往是局部极端值，GCN 的消息传递机制可能平滑了局部热点 | ⭐⭐⭐ |

### 4.2 损失函数架构建议

当前损失函数架构的潜在改进方向：

1. **增大峰值温升损失权重**：当前 `λ_peak_temperature_rise = 0.01`，可能不足以驱动网络关注峰值精度的优化。建议尝试 0.1–1.0 范围内调节。

2. **引入峰值定位损失**：除峰值温度值外，加入峰值出现的时间帧/空间位置的监督损失。

3. **添加 hard example mining**：对高 Q 区域（误差大）的样本增加采样权重。

4. **调整 smoothness 正则化强度**：当前 loss_smooth 几乎不收敛，可能是权重设置过高，压制了网络对局部细节的学习能力。建议降低平滑损失的贡献比例。

5. **引入温度范围约束**：显式加入 T_min ≥ T_amb 的物理约束损失，防止出现低于环境温度的不合理预测。

---

## 5. 结论与建议

### 5.1 训练成效

✅ **正向成果：**
- PDE 物理损失和出流损失收敛良好（降低 >7,000 倍）
- 温度预测的物理合理性逐步改善（T_min 从负值修正至接近环境温度）
- 迎风权重实现了自适应的差异化调节
- 训练过程稳定，未出现梯度爆炸或损失震荡

### 5.2 待解决问题

❌ **关键问题：**
- 峰值温度系统性低估 ~139 °C，R² 为负值
- 高功率区域误差尤为严重（MAPE ~33%）
- 平滑性损失几乎不收敛
- 模型尚不能替代 FEM 进行高精度温度预测
