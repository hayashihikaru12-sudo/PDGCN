# 峰值温升监督方案

## 1. 目标

当前 PDGCN 纯物理训练可以稳定收敛，但与 FEM 对比时存在明显的峰值温度系统性低估。为避免节点级 FEM 温度监督引入局部震荡，建议只引入低维的**峰值温升监督**。

该方案只约束每一帧或每个监督步的峰值温升，不要求预测温度场在每个节点上逐点贴合 FEM。

---

## 2. 核心思想

温度监督目标从节点级场：

$$
T_{\mathrm{pred},i} \approx T_{\mathrm{FEM},i}
$$

改为峰值温升级标量：

$$
\Delta T_{\mathrm{peak,pred}} \approx \Delta T_{\mathrm{peak,FEM}}
$$

其中：

$$
\Delta T_{\mathrm{peak}} = T_{\mathrm{peak}} - T_{\mathrm{amb}}
$$

这样监督信号只校正模型的峰值幅值偏差，不强制模型学习 FEM 的局部节点误差或高频数值纹理。

---

## 3. 峰值定义

不建议直接使用硬最大值：

$$
T_{\mathrm{peak}} = \max_i T_i
$$

因为硬 `max` 的梯度只作用于单个节点，容易导致训练不稳定。

推荐使用 `top-k mean` 作为平滑峰值近似：

$$
T_{\mathrm{peak}}^{(k)}
=
\frac{1}{k}
\sum_{i \in \mathrm{TopK}(T)} T_i
$$

其中 $\mathrm{TopK}(T)$ 表示温度最高的 $k$ 个节点集合。

对于当前约 2910 个节点的单层曲面，可先取：

$$
k = 15 \sim 50
$$

即约为节点数的 $0.5\% \sim 2\%$。

---

## 4. 无量纲峰值温升监督

当前训练中温度无量纲化为：

$$
T^* = \frac{T - T_{\mathrm{amb}}}{\Delta T_0}
$$

因此峰值温升监督建议直接在无量纲空间中计算：

$$
\Delta T_{\mathrm{peak}}^*
=
\frac{T_{\mathrm{peak}} - T_{\mathrm{amb}}}{\Delta T_0}
$$

由于 $T^*$ 本身已经是相对环境温度的无量纲温升，因此可直接写为：

$$
\Delta T_{\mathrm{peak}}^*
=
\mathrm{TopKMean}(T^*)
$$

预测值与 FEM 标签分别为：

$$
\Delta T_{\mathrm{peak,pred}}^*
=
\mathrm{TopKMean}(T_{\mathrm{pred}}^*)
$$

$$
\Delta T_{\mathrm{peak,FEM}}^*
=
\mathrm{TopKMean}(T_{\mathrm{FEM}}^*)
$$

峰值监督损失为：

$$
\mathcal{L}_{\mathrm{peak}}
=
\left(
\Delta T_{\mathrm{peak,pred}}^*
-
\Delta T_{\mathrm{peak,FEM}}^*
\right)^2
$$

---

## 5. 总损失函数

原始物理损失为：

$$
\mathcal{L}_{\mathrm{physics}}
=
\mathcal{L}_{\mathrm{pde}}
+
\lambda_{\mathrm{outflow}}\mathcal{L}_{\mathrm{outflow}}
+
\lambda_{\mathrm{smooth}}\mathcal{L}_{\mathrm{smooth}}
$$

加入峰值温升监督后：

$$
\mathcal{L}_{\mathrm{total}}
=
\mathcal{L}_{\mathrm{physics}}
+
\lambda_{\mathrm{peak}}\mathcal{L}_{\mathrm{peak}}
$$

建议初始取值：

$$
\lambda_{\mathrm{peak}} = 10^{-3} \sim 10^{-2}
$$

如果峰值偏差仍然明显，可逐步增大；如果训练 loss 出现明显尖峰或 rollout 不稳定，应减小该权重。

---

## 6. 权重 Warmup

为避免训练初期峰值监督破坏 PDE 收敛，建议对 $\lambda_{\mathrm{peak}}$ 使用线性 warmup：

$$
\lambda_{\mathrm{peak}}(e)
=
\lambda_{\mathrm{peak,max}}
\cdot
\min\left(1, \frac{e}{E_{\mathrm{warmup}}}\right)
$$

其中：

- $e$：当前 epoch
- $E_{\mathrm{warmup}}$：warmup epoch 数
- $\lambda_{\mathrm{peak,max}}$：目标峰值监督权重

建议：

$$
E_{\mathrm{warmup}} = 20 \sim 50
$$

---

## 7. 训练接入方式

推荐保留当前物理训练流程，只在每一步预测得到 $T_{\mathrm{pred},n+1}^*$ 后额外读取 FEM 的下一帧温度 $T_{\mathrm{FEM},n+1}^*$，并计算峰值监督。

单步流程：

```text
T_current*
  -> 显式热源模块
  -> PD-GCN 无源面内输运
  -> T_pred_next*
  -> 计算 PDE loss
  -> 计算 top-k peak loss
  -> 合并 total loss
```

监督标签仍可来自：

```text
fem/temperature [T, N, 1]
```

但训练时只使用其 `top-k` 峰值统计，不计算节点级 MSE。

---

## 8. 推荐配置

第一组实验建议：

```json
{
  "peak_supervision": {
    "enabled": true,
    "temperature_dataset": "fem/temperature",
    "valid_mask_dataset": "fem/valid_mask",
    "topk": 30,
    "lambda_peak": 0.001,
    "warmup_epochs": 30,
    "mode": "rollout"
  }
}
```

该配置与节点级 `supervision` 互斥，不能同时启用。

如果峰值修正不足：

```text
lambda_peak: 0.001 -> 0.003 -> 0.01
```

如果训练震荡明显：

```text
lambda_peak: 0.01 -> 0.003 -> 0.001
topk: 15 -> 30 -> 50
warmup_epochs: 30 -> 50
```

---

## 9. 预期效果与局限

预期效果：

- 改善 FEM 峰值温升系统性低估问题。
- 比节点级 FEM MSE 更稳定，不容易引入局部高频震荡。
- 监督信号低维、清晰，便于解释和调参。

局限：

- 只能直接约束峰值幅值，不保证热斑位置完全一致。
- 不保证热影响区面积、温度分布形状与 FEM 完全一致。
- 若峰值对齐后热斑过尖或过窄，后续可追加 P95/P99 或热区均值监督。

---

## 10. 建议优先验证指标

训练完成后重点比较：

```text
FEM ΔT_peak vs PDGCN ΔT_peak
峰值温升 Bias
峰值温升 MAE
峰值温升 RMSE
峰值温升 MAPE
不同 Q 分组下的相对误差
rollout 稳定性
```

如果峰值温升 MAPE 明显下降，且 rollout 温度场无新增震荡，则说明峰值温升监督有效。

训练 history 和 monitor 中对应字段为：

```text
loss_peak_temperature_rise
peak_temperature_rise_pred
peak_temperature_rise_fem
peak_temperature_rise_error
peak_temperature_rise_abs_error
peak_temperature_rise_rmse
lambda_peak_temperature_rise
```
