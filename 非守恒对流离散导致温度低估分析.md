# 非守恒对流离散导致系统性温度低估分析

## 1. 问题定位

PDGCN 推理阶段峰值温度系统性低估 FEM 约 $-139^\circ\text{C}$，误差随热流 $Q$ 增大、扫描速度 $V$ 减小而单调递增（$\text{MAPE}$ 从 $20\%$ 升至 $33\%$）。根因之一为 PDE 残差中对流项的**非守恒离散格式**。

当前代码已将该项修复为守恒 FVM 上风边通量格式，见 `pde/residual.py`。

## 2. 修复前对流离散公式

修复前 PDE 残差中，节点 $i$ 的对流项为：

$$
\mathcal{C}_i = \sum_{j \in \mathcal{N}(i)} v_{\text{scan}}^* \,
\mathrm{ReLU}(\cos\theta_{ij}) \,
\frac{T_i^* - T_j^*}{d_{ij}^*}
$$

其中 $\cos\theta_{ij} = \mathbf{v}_{\text{tangent},i} \cdot \frac{\mathbf{x}_i - \mathbf{x}_j}{d_{ij}}$，表示扫描方向在节点 $i$ 处与边 $j \to i$ 方向的夹角余弦。$\mathrm{ReLU}$ 截断使**仅上游边**对节点 $i$ 产生对流贡献。

## 3. 非守恒性分析

### 3.1 单条边的热量失衡

对边 $U \to P$（$U$ 在上游，$P$ 在下游）：

$$
\begin{aligned}
&\cos\theta_P > 0 &: \quad &\mathcal{C}_P = +v\frac{T_P - T_U}{d} \quad \text{(计入 }P\text{)} \\
&\cos\theta_U < 0 &: \quad &\mathcal{C}_U = 0 \quad \text{(截断，不计入 }U\text{)}
\end{aligned}
$$

**结论：$P$ 的方程中出现了由 $U\to P$ 单边差分产生的对流贡献，但 $U$ 的方程中没有相反贡献。** 因此该离散不是边通量守恒形式。

### 3.2 温度峰值处的非守恒额外冷却

设峰值节点 $P$ 温度 $T_P \gg T_U$（$U$ 为上游邻居）：

$$
\mathcal{C}_P = \sum_{j:\,\cos\theta>0} v \cdot \frac{T_P - T_j}{d} \quad\text{所有项 } >0
$$

当前残差符号约定为：

$$
R = \frac{T_{\mathrm{next}}-T_{\mathrm{current}}}{\Delta t} + \mathcal{C} - \mathrm{Pe}^{-1}\mathcal{D}.
$$

因此 $\mathcal{C}_P>0$ 会在残差最小化时强迫瞬态项为负，即强迫 $T_{\mathrm{next}}<T_{\mathrm{current}}$。这不是物理守恒流出造成的冷却，而是只计入 receiver、没有 sender 反向贡献的**非守恒额外冷却**。

### 3.3 模型补偿导致系统性低估

PDGCN 学习预测 $\Delta T_{\text{inplane}}^*$ 来满足该残差：

$$
T_{\text{next}}^* = T_{\text{current}}^* + \Delta T_Q^* + \Delta T_{\text{inplane}}^*
$$

由于非守恒额外冷却 $\propto (T_P - T_{\text{upstream}}) \propto \Delta T_{\text{peak}}$，峰值越高，PDE residual 对偏冷解的牵引越强。该机制直接解释误差与 $Q$ 正相关、与 $V$ 负相关。

## 4. 为何不能简单改为 $\cos\theta$

若将 $\mathrm{ReLU}(\cos\theta)$ 直接替换为 $\cos\theta$，对有向边对 $(j \to i)$ 和 $(i \to j)$：

$$
\begin{aligned}
\text{边 }j \to i &: \quad \cos\theta_{ij} > 0, \quad \mathcal{C}_i = +v \cdot \frac{T_i - T_j}{d} \\
\text{边 }i \to j &: \quad \cos\theta_{ji} \approx -\cos\theta_{ij} < 0, \quad \mathcal{C}_j = -v \cdot \frac{T_j - T_i}{d} = +v \cdot \frac{T_i - T_j}{d}
\end{aligned}
$$

**两条边给 $i$ 和 $j$ 的贡献符号相同、大小相等。** 这仍然不是守恒边通量，并会在高 Peclet 数下引入中心差分类数值失稳；因此不能只把 `ReLU(cos_theta)` 替换成 `cos_theta`。

## 5. 修复方案：守恒 FVM 上风格式

### 5.1 离散公式

将基于节点差分的 Galerkin 格式改为基于边通量的守恒 FVM 格式。对每条有向边 $j \to i$：

**Step 1：确定上游节点温度**

$$
T_{\text{upwind}} = \begin{cases}
T_j^* & \text{if } \cos\theta_{ij} > 0 \quad (j \text{ 在上游}) \\
T_i^* & \text{if } \cos\theta_{ij} \le 0 \quad (i \text{ 在上游})
\end{cases}
$$

**Step 2：计算守恒通量并分配到两端节点**

$$
\mathcal{F}_{j \to i} = v_{\text{scan}}^* \cdot \cos\theta_{ij} \cdot \frac{T_{\text{upwind}}}{d_{ij}^*}
$$

$$
\mathcal{C}_j \mathrel{+}= \mathcal{F}_{j \to i}, \qquad \mathcal{C}_i \mathrel{-}= \mathcal{F}_{j \to i}
$$

其中残差采用 `transient + convection - diffusion = 0` 的符号约定。$\mathcal{F}_{j\to i}>0$ 表示通量沿 $j\to i$ 流动，所以 sender $j$ 是流出端，receiver $i$ 是流入端。

### 5.2 守恒性验证

对于任意边，sender 和 receiver 的贡献符号相反：

$$
\mathcal{C}_j + \mathcal{C}_i = \mathcal{F}_{j \to i} + (-\mathcal{F}_{j \to i}) = 0
$$

全局 $\sum_i \mathcal{C}_i = 0$ —— 对流项不产生净热量，严格守恒。

### 5.3 热点流出验证

峰值 $P$（高温）位于流出端，下游邻居为 $D$，$v \to$：

$$
T_{\text{upwind}} = T_P \quad (\cos\theta > 0, P\text{ 在上游})
$$

$$
\mathcal{C}_P = +v \cdot \frac{T_P}{d}, \quad \mathcal{C}_D = -v \cdot \frac{T_P}{d}
$$

- $P$ 是流出端，$\mathcal{C}_P>0$ 会驱动 $P$ 降温；
- $D$ 是流入端，$\mathcal{C}_D<0$ 会接收来自热点的热量；
- 两端贡献相加为 0，热点冷却不再伴随净热量丢失。

物理含义与对流冷却峰值、输运热量的过程完全一致。

## 6. 改动范围

| 文件 | 改动内容 |
|---|---|
| `pde/residual.py` | 将对流项改为 `_conservative_upwind_convection`，由 sender/receiver 双端更新 |
| `pde/tests/test_residual.py` | 更新手算残差，并新增全局守恒与热点流出符号测试 |

约 30–40 行 Python，不涉及模型结构、训练循环或配置文件。

## 7. 与其他修复措施的协同

| 措施 | 解决的问题 | 关系 |
|---|---|---|
| **守恒 FVM 对流**（本文） | 非守恒额外冷却 | 根因修复 |
| `gradient_regularization = 0` | 梯度惩罚压制高温峰值 | 消除辅助损失干扰 |
| `lambda_peak` 提升至 $0.1$–$0.5$ | 峰值监督信号过弱 | 增强纠偏能力 |
| `residual_time_scheme = "explicit"` | 后向残差可能自洽到偏冷解 | 建议作为消融项验证 |

后 3 项属于训练策略消融，不与守恒 FVM 根因修复绑定。
