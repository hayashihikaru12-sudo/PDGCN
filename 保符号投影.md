# 保符号投影

## 目的

保符号投影用于替换原来的“均值扣除投影”，作为推理阶段或调试阶段的硬约束兜底。它只缩放正增量，不把原本预测升温的节点改成降温。

当前采样点是均匀的，因此不使用面积权重，所有内部节点等权处理。

## 定义

PD-GCN 的无源面内原始输出为：

$$
\Delta T_i^{\mathrm{raw}},
\quad
i \in \Omega_{\mathrm{int}}
$$

其中 $\Omega_{\mathrm{int}}$ 表示内部节点集合，不包含入口、侧边和出口边界节点。

定义正增量和负增量幅值：

$$
p_i=\max\left(\Delta T_i^{\mathrm{raw}},0\right)
$$

$$
n_i=\max\left(-\Delta T_i^{\mathrm{raw}},0\right)
$$

则：

$$
\Delta T_i^{\mathrm{raw}}=p_i-n_i
$$

由于采样点均匀，正增量总量和负增量总量直接取节点和：

$$
S_+=\sum_{i\in\Omega_{\mathrm{int}}}p_i
$$

$$
S_-=\sum_{i\in\Omega_{\mathrm{int}}}n_i
$$

## 投影规则

若：

$$
S_+\le S_-
$$

说明无源面内项没有产生净升温，保持原输出不变：

$$
\Delta T_i^{\mathrm{proj}}=\Delta T_i^{\mathrm{raw}}
$$

若：

$$
S_+>S_-
$$

则只缩放正增量：

$$
\alpha=\frac{S_-}{S_+ + \varepsilon}
$$

$$
\Delta T_i^{\mathrm{proj}}=\alpha p_i-n_i
$$

其中 $\varepsilon$ 是防止除零的小量。

## 性质

投影后满足无源非产热约束：

$$
\sum_{i\in\Omega_{\mathrm{int}}}\Delta T_i^{\mathrm{proj}}\le 0
$$

并且保持符号：

$$
\Delta T_i^{\mathrm{raw}}>0
\Rightarrow
\Delta T_i^{\mathrm{proj}}\ge 0
$$

$$
\Delta T_i^{\mathrm{raw}}<0
\Rightarrow
\Delta T_i^{\mathrm{proj}}<0
$$

因此，低温节点若原本被模型预测为升温，投影后最多被压小到接近 $0$，不会被强行改成降温。

## 退化情况

若负增量总量接近 $0$：

$$
S_-\approx 0
$$

则：

$$
\alpha\approx 0
$$

此时所有正的面内增量都会被压到接近 $0$。这说明保符号投影仍能防止自产热，但不能主动生成合理的降温区域；模型仍需要通过软能量约束学习“升温区”和“降温区”的配对结构。

## 简化代码

```python
def project_sign_preserving_delta(delta, internal_mask, eps=1e-12):
    projected = delta.clone()
    internal = projected[internal_mask]

    positive = torch.relu(internal)
    negative = torch.relu(-internal)

    positive_sum = positive.sum()
    negative_sum = negative.sum()

    if positive_sum > negative_sum:
        alpha = negative_sum / positive_sum.clamp_min(eps)
        internal = alpha * positive - negative
        projected[internal_mask] = internal

    return projected
```

## 当前代码配置

训练阶段默认关闭硬投影：

```json
"non_heating_projection": false
```

需要推理兜底时，可显式开启：

```json
"non_heating_projection": true
```
