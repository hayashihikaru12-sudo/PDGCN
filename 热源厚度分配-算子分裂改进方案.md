# 热源厚度分配算子分裂改进方案

## 1. 问题定位

### 1.1 当前算子分裂

当前多层推理的每步更新顺序为：

$$
\boxed{T^n \xrightarrow{\;+\Delta T_Q\;} T^\dagger \xrightarrow{\;\text{PDGCN}\;} T^\ddagger \xrightarrow{\;\text{FDM}\;} T^{n+1}}
$$

写成公式，对第 $l$ 层：

$$
\begin{aligned}
T^\dagger_l &= \text{BC}\Big[T_l^n + \delta_{l,0} \cdot \Delta T_Q^n\Big] \\[4pt]
T^\ddagger_l &= \text{BC}\Big[T^\dagger_l + \mathcal{F}_\theta\big(T^\dagger_l\big)\Big] \\[4pt]
T^{n+1} &= \text{ImplicitFDM}\big(T^\ddagger,\; T_\text{bottom}^*\big)
\end{aligned}
$$

### 1.2 根本矛盾

PDGCN 的训练物理是封闭二维曲面——热源施加后，面内输运是**唯一的演化机制**：

$$
T^{n+1} = \text{BC}\Big[T^n + \Delta T_Q^n + \mathcal{F}_\theta\big(T^n + \Delta T_Q^n\big)\Big]
$$

但推理时，FDM 在 PDGCN 之后再次修改温度场：

$$
T^{n+1}_0 = \frac{T^\ddagger_0 + C_n \cdot T^{n+1}_1}{1 + C_n} \;<\; T^\ddagger_0
$$

**PDGCN 的权重被训练为"我是温度的唯一演化算子"，但推理时它的输出被 FDM 抽走了热量。** 顶部层每步都因厚度散热损失温升，自回归累积导致 $\sim 150^\circ\text{C}$ 的系统性偏低。

---

## 2. 改进方案：热源厚度分配

### 2.1 核心思想

**将 FDM 从"扩散温度"改为"扩散热源"。** FDM 只负责把顶层激光热源沿厚度方向分配给各层，不再参与温度更新。

### 2.2 物理依据

三维热方程的厚度扩散项在热源主导场景下的近似分解：

$$
\frac{\partial T}{\partial t} = \alpha_\parallel \nabla^2_\parallel T + \alpha_z \frac{\partial^2 T}{\partial z^2}
$$

厚度扩散可视为对热源的**空间再分配**而非对温度的持续均衡。将 $\Delta T_Q$ 沿 $z$ 方向扩散后，每层独立运行面内输运，物理一致性更高。

### 2.3 公式对比

| | 当前方案 | 改进方案 |
|---|---|---|
| **FDM 作用对象** | 温度场 $T$ | 热源项 $\Delta T_Q$ |
| **FDM 作用时机** | PDGCN 之后 | PDGCN 之前 |
| **厚度散热** | 每步从顶层抽热 | 通过源分配隐式体现 |
| **PDGCN 训练一致性** | ❌ 不一致 | ✅ 一致 |

#### 当前方案公式

$$
\boxed{
\begin{aligned}
\text{Step 1 — 显式源（仅顶层）:}&\quad \Delta T_{Q,l}^n = \delta_{l,0} \cdot \eta \Pi_Q dt^* q_{\text{surf}}^{*n} \\[4pt]
\text{Step 2 — 面内输运（所有层）:}&\quad T^\ddagger_l = \text{BC}\Big[T_l^n + \Delta T_{Q,l}^n + \mathcal{F}_\theta\big(T_l^n + \Delta T_{Q,l}^n\big)\Big] \\[4pt]
\text{Step 3 — 厚度散热:}&\quad T^{n+1} = \text{ImplicitFDM}\big(T^\ddagger,\; T_\text{bottom}^*\big)
\end{aligned}}
$$

> ⚠️ Step 3 把 PDGCN 的预测从顶层"抽走"，训练时无此步骤。

#### 改进方案公式

$$
\boxed{
\begin{aligned}
\text{Step 1 — 总热源聚集（仅顶层）:}&\quad S_l^n = \delta_{l,0} \cdot \eta \Pi_Q dt^* q_{\text{surf}}^{*n} \\[4pt]
\text{Step 2 — 热源厚度扩散:}&\quad \widetilde{S}^n = \text{ImplicitFDM}\big(S^n,\; 0\big) \\[4pt]
\text{Step 3 — 每层独立面内输运:}&\quad T^{n+1}_l = \text{BC}\Big[T_l^n + \widetilde{S}^n_l + \mathcal{F}_\theta\big(T_l^n + \widetilde{S}^n_l\big)\Big]
\end{aligned}}
$$

> ✅ Step 3 与 PDGCN 训练时的物理公式**完全一致**——每层看到的都是"本地热源 + 当前温度 → 面内输运 → 下一帧温度"。

---

## 3. 关键细节

### 3.1 热源 FDM 的边界处理

$$
\text{ImplicitFDM}\big(S^n,\; \text{bottom}=0\big)
$$

- 底部边界设为 0：底层模具不参与热源分配（物理上模具散热是边界条件，不是源项汇）
- 顶部自由边界：无额外约束，热源自然向下扩散
- 热源矩阵的隐式求解保证了无条件稳定，$C_n \approx 0.25$ 时耦合温和

### 3.2 能量守恒

总热源注入量在厚度方向守恒（忽略底层微小吸收）：

$$
\sum_{l=0}^{L-2} \widetilde{S}^n_l \;\approx\; \sum_{l=0}^{L-2} S^n_l \;=\; \underbrace{\eta \Pi_Q dt^* q_{\text{surf}}^{*n}}_{\text{顶层原始热源}}
$$

步与步之间不再有"PDGCN 温升被抽走"的能量损失。

---

## 4. 总结

```
改进前:  Source → PDGCN → FDM(温度)    ← PDGCN和FDM互相"打架"
改进后:  Source → FDM(源) → PDGCN      ← 各层独立，物理自洽

核心改动: FDM 输入从"温度"改为"源项"，其余结构不变
```
