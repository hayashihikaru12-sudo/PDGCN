### 3.1 方案 1：将 $q^*$ 加入节点特征

#### 动机

当前节点特征为 $[x^*, y^*, z^*, f_x, f_y, f_z, T^*]$，共 7 维。$q^*$ 仅存储在 `graph.q_surface_star` 中用于显式源计算，PDGCN 从未读取。网络无法"知道"哪些节点正在被加热、加热强度多大。

#### 方案

将无量纲表面热流 $q^*$ 作为第 8 维拼入节点特征：

```text
[x*, y*, z*, fx, fy, fz, T*, q*]
```

其中：

$$
q^* = \frac{q''}{Q_0}
$$

#### 物理直觉

- 高 $q^*$ 节点 → 预期更大温度梯度 → 网络应预测更强的扩散输运
- $q^*$ 的空间分布模式与温度场演化存在可学习的关联
- 不同时间步 $q^*$ 的变化模式帮助网络理解加热历史

#### 实现要点

- `GpuFeatureBuilder.build()` 中 `self.x` 缓冲区维度扩展至 `[N, 8]`
- `PDGCNConfig.node_input_size` 从 7 → 8
- `Encoder` 的 `node_encoder` 输入维度自动跟随配置
- 消融实验：配置项 `include_q_in_features` 即可开关

#### 风险

极低。仅增加一维输入，不改变架构逻辑。可随时回退。

---

### 3.2 方案 2：将 $\Delta T_Q^*$ 加入节点特征

#### 动机

当前流程为：

$$
T_{\text{source}}^* = \mathcal B(T_{\text{current}}^* + \Delta T_Q^*)
$$

网络看到的 $T_{\text{source}}^*$ 已经混合了加热效应和之前温度，无法区分"这个节点的温度来自当前步加热"还是"来自之前的演化"。$\Delta T_Q^*$ 提供了关键的**时间差分信息**。

#### 方案

将每步计算的显式源温升 $\Delta T_Q^*$ 也作为节点特征输入：

```text
[x*, y*, z*, fx, fy, fz, T*, q*, ΔT_Q*]
```

其中：

$$
\Delta T_Q^* = \eta \Pi_s \Delta t^* q^*
$$

$$
\Pi_s = \frac{Q_0 L_0}{\rho C_p v_0 h_{\text{eff}} \Delta T_0}
$$

#### 物理直觉

- $\Delta T_Q^*$ 告诉网络"这个节点在这一步被加热了多少"
- 加热剧烈的节点可能需要更强的扩散来散热
- 非加热区域的温度变化完全来自面内输运——网络可以据此分区域调整预测策略

#### 实现要点

- `node_input_size` 扩展至 9
- `GpuFeatureBuilder.build()` 接受额外参数 `delta_T_source_star`
- 训练循环中在调用 `build()` 前计算 `delta_T_source_star` 并传入
- 推理路径同步修改